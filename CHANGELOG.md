# Changelog

## v6.44 (2026-08-25, 데모 계정 문구 정리 + 진행중 상태 데모 계정 추가)

김민준의 데모 로그인 안내 문구 "인도 완료 · 신차 케어 서비스 진행중"이, "진행중"이 신차인도서비스 계약 자체가 아니라 완전히 별개인 신차 케어 서비스 쪽 얘기라는 걸 명확히 구분하지 않아 헷갈렸다 — "내 계약 확인"(신차인도서비스만 보여주는 화면)에 들어가면 두 건 다 "완료"만 보이니 문구와 안 맞는 것처럼 보였다.

**변경**: 문구를 "신차인도서비스는 완료 · 신차 케어 서비스는 별도로 진행중"으로 명확히 나눴다. 그리고 신차인도서비스 자체가 실제로 진행중(정확히는 DELIVERED — 도착은 했지만 카마스터 개인수령확인·고객 최종인수승인 둘 다 아직 안 끝난 상태)인 데모 계정을 하나 추가했다 — 박지훈(010-7777-3000, K5, 도현매니저 담당). DELIVERED는 IN_TRANSIT과 달리 만료 타이머가 없는 정적 상태라, 시드로 심어둬도 시간이 지나도 저절로 안 바뀐다.

**영향받은 파일**: `store.js`(`getDemoCustomers()` 문구 수정 + 3번째 항목 추가, `DEFAULT_RESERVATIONS`에 박지훈 건 추가). 박지훈도 김민준과 같은 카마스터(k1) 담당으로 둬서, `test_booking_approval.js`의 "카마스터 담당 목록 시드 건수" 기대값을 1건→2건(따라서 전체 3건→4건)으로 맞췄다. 9개 테스트 전부 통과. 브라우저로 박지훈 데모 로그인 → "내 계약 확인"에 "탁송완료(확인대기)"로 정확히 뜨는 것을 확인했다.

## v6.43 (2026-08-25, 시드 데이터 항상 보장 + 내 계약 확인 화면 재구성)

두 가지를 함께 고쳤다 — 둘 다 "시드 데이터(v6.40)가 왜 안 보이나"로 이어지는 문제였다.

**① 기존 localStorage에도 시드가 항상 들어가도록 수정**: `DEFAULT_RESERVATIONS`/`DEFAULT_CARE_ORDERS`가 `_emptyStore()`를 거칠 때(=localStorage가 완전히 빈 상태)만 들어가는 구조였다 — 그래서 이 앱을 이미 써봐서 localStorage에 예전 데이터가 남아있는 브라우저(이번 세션 내내 테스트해온 개발 환경 포함)에서는 raw가 이미 있어 `_emptyStore()`가 아예 실행되지 않고, 시드가 영원히 안 들어갔다. `Store.load()`에서 raw 유무와 무관하게, 시드 id가 없으면 멱등하게 끼워넣도록 고쳤다 — 완전히 빈 저장소를 흉내낸 테스트로 정상 주입을 확인했다.

**② "내 계약 확인 / 이력 조회" 화면 재구성**: 이미 로그인된 상태인데도 전화번호 입력창이 그대로 남아있어 "이게 내 번호인지 카마스터 번호인지" 헷갈린다는 지적을 반영했다. 이 화면은 로그인(loggedInCustomer)해야만 들어올 수 있어 `loggedInPhone`이 항상 있으므로, 이제 그 번호로 곧바로 목록을 보여준다 — 검색창은 "다른 연락처로 조회하기"를 눌렀을 때만(가족 등 대신 조회하는 드문 경우) 펼쳐진다. 계약이 없으면 "아직 등록된 계약이 없습니다"와 함께 계약내역 등록하기 버튼을 보여준다(대신 조회 중일 때는 이 버튼을 뺐다 — 내 계약이 아니므로).

**③ 시드 이력을 2건으로**: 김민준에게 6일 전 완료 건 외에, 130일 전에 이미 카마스터 평가까지 끝난 오래된 완료 건(쏘나타, 준호쓰카 담당)을 하나 더 심어서 "내 계약 확인"이 여러 건짜리 진짜 이력 목록으로도 보이게 했다.

**영향받은 파일**: `store.js`(`load()`에 멱등 시드 주입, `DEFAULT_RESERVATIONS`에 김민준 두 번째 이력 추가), `customer.js`(`renderHistory` 재구성). 이 재구성으로 "로그인 연락처를 직접 검색"하던 기존 테스트 3개 파일의 관련 단계가 필요 없어져(자동으로 뜸) 제거했다. 9개 테스트 전부 통과. 브라우저로 직접 확인: 완전히 빈 저장소·낡은 저장소(시드 없이 저장된 상태) 둘 다에서 시드가 정상 주입되는 것, 데모 로그인 후 "내 계약 확인"에 검색 없이 바로 2건이 뜨는 것을 확인했다.

## v6.42 (2026-08-25, 내 계약 확인 — 로그인 연락처로 자동 조회)

"내 계약 확인 / 이력 조회"가 로그인 시점에 이미 아는 연락처(`loggedInPhone`)를 전혀 재사용하지 않고, 이 화면 전용의 별도 상태(`historyPhone`, 이 화면에서 마지막으로 검색한 값)만 기억하고 있었다. 그래서 로그인(또는 데모 계정 선택) 직후 처음 이 화면에 들어가면 검색창이 비어 있어 아무것도 안 보였다 — 시드 데이터(v6.40)가 카마스터·시공업체 화면에는 잘 반영되는데 정작 고객 자신의 화면에서는 확인이 안 되는 것처럼 보이는 원인이었다.

**변경**: `renderHistory()`에서 `historyPhone`이 비어 있으면(이 화면에서 검색한 적이 아직 없으면) `loggedInPhone`으로 기본값을 채운다. 기존의 "historyPhone이 있으면 자동 조회" 로직과 맞물려, 로그인 직후 이 화면에 들어가자마자 본인 계약 목록이 바로 보인다. 가족 등 다른 번호로 조회하고 싶으면 그대로 다시 입력하면 되고, 그 값은 계속 기억된다(sessionStorage).

**영향받은 파일**: `customer.js`(`renderHistory` 진입부 3줄). 9개 테스트 전부 통과. 브라우저로 데모 로그인 직후 재검색 없이 "내 계약 확인"에 바로 진입해 목록이 뜨는 것을 확인했다.

## v6.41 (2026-08-25, 구매자 로그인 — 빈 값 로그인 차단)

"간편 로그인"이 이름·연락처가 빈 칸이어도 그냥 눌려서, 신원 없는 상태로 넘어갈 수 있었다. 데모 계정 드롭다운(v6.40)이 생긴 지금은 "빈 값으로도 들어가지는" 지름길이 필요 없다 — 진짜 신규 가입을 테스트하려면 실제 값을 입력해야 하고, 이미 있는 사례를 보려면 데모 계정을 고르면 된다.

**변경**: 이름 2자 이상 + 연락처가 `010-XXXX-XXXX` 형식과 맞을 때만 로그인 버튼이 활성화되도록 실시간 검증을 추가했다(다른 폼들과 같은 disabled 토글 패턴). `tryCustomerLogin()` 자체에도 같은 조건의 가드를 한 번 더 넣어, 버튼이 비활성 상태에서도 함수가 직접 호출될 경우(데모 드롭다운의 자동 로그인 등)를 대비했다. 데모 계정 드롭다운은 유효한 값을 채운 뒤 곧바로 호출하므로 영향 없다. 버튼 라벨도 "간편 로그인" → "로그인"으로 바꿨다 — 이제 빈 값으로 넘어가는 지름길이 아니라 이 화면의 유일한 로그인 경로이므로 "간편"이라는 수식어가 안 맞았다.

**영향받은 파일**: `customer.js`(`renderLoginMock`/`tryCustomerLogin`). 이 검증 때문에 "빈 칸으로 로그인 버튼 클릭"을 전제로 하던 기존 테스트 8개 파일(9곳)이 깨져서, 로그인 직전에 `#login-name`/`#login-phone`을 채우도록 고쳤다(대부분 그 테스트가 이후 `#rq-name`/`#rq-phone`에 채우는 값과 동일하게 맞춤). `test_input_stability.js`는 로그인값이 계약 등록 폼의 이름 필드에도 자동 채워지는 부작용이 있어, 타이핑 검증 전에 필드를 한 번 비우도록 추가했다. 버튼 라벨이 "로그인"으로 바뀌면서 화면 제목("구매자 로그인")·설명 문구("...로그인합니다")와도 텍스트가 겹치게 돼, 테스트의 `text=` 셀렉터를 전부 `#login-submit`(id) 기반으로 바꿔 라벨 문구에 더 이상 의존하지 않게 했다. 9개 테스트 전부 통과. 브라우저로 직접 확인: 빈 칸 상태에서 버튼 비활성, 이름+연락처를 채우면 활성화되는 것을 확인했다.

## v6.40 (2026-08-25, 구매자 데모 로그인 · 시드 예약 · 카마스터 계약 대리 등록)

매번 계약등록→승인→확정을 손으로 밟아야만 다음 기능(신차 케어 서비스, 진행중인 배송 UI 등)을 확인할 수 있어서, 데모/테스트가 불편했다. 세 가지를 한 번에 손봤다.

**① 구매자 데모 로그인 (NEW)**: 카마스터/시공업체/배송기사/관리자 4개 역할에만 있던 "데모 계정으로 빠른 로그인"이 구매자 화면에는 없었다 — 애초에 구매자는 계정 엔티티 자체가 없어서다(예약은 매번 입력한 이름+연락처로만 매칭된다). `customer.html` 로그인 화면에 같은 패턴의 드롭다운을 추가하고, `Store.getDemoCustomers()`가 반환하는 고정 목록(아래 ②의 시드 고객 2명)을 그대로 노출한다. 신규 가입 테스트는 기존처럼 입력창에 직접 적으면 된다 — 아무것도 제거하지 않았다.

**② 시드 예약/케어 주문 (NEW)**: `reservations: []`, `careOrders: []`로 완전히 빈 상태에서 시작하던 것을, 기가입 고객 2명의 예약을 `DEFAULT_RESERVATIONS`에 미리 심어두는 것으로 바꿨다 — 김민준(010-7777-1000, k1 담당, CONFIRMED까지 완료 + 진행중인 신차 케어 서비스 주문 1건 연결)과 이서연(010-7777-2000, k2 담당, CUSTOMIZING 진행중 + 배송 상세정보 이미 게시됨)이다. id를 `10-202601-9xxx`/`20-202601-9xxx` 대역에 고정해, seq로 자동 채번되는 실제 예약과 절대 겹치지 않는다.

**③ 카마스터의 계약 대리 등록 (NEW)**: 관리자의 "계약 대리 등록"과 똑같이 `Store.createContractRecordDirect()`를 재사용하되, 담당 카마스터를 고르는 절차 없이 로그인한 카마스터 본인으로 고정된다. 이 메서드가 만드는 예약은 원래도 `'계약등록'` 단계에서 시작하므로(카마스터 승인 단계 자체가 없음) — 본인이 이미 검토하고 입력한 것이니 자연스럽게 그 단계를 건너뛰고 고객 확인만 남는다(대시보드 KPI로 "승인 필요 0 · 고객 확인 대기 +1"이 되는 것으로 확인). 연락처를 입력하는 즉시 `Store.getReservationsByPhone()`으로 조회해 "기존 고객입니다 — 이전 계약 이력 N건" / "처음 등록하는 신규 고객입니다"를 실시간으로 보여준다.

**영향받은 파일**: `store.js`(`DEFAULT_RESERVATIONS`/`DEFAULT_CARE_ORDERS` 신설, `_emptyStore()`가 이를 참조하도록 수정, `getDemoCustomers()` 신설), `customer.js`(`renderLoginMock()`에 데모 드롭다운), `karmaster.js`(`renderKmNewContractForm()` 신설 + 대시보드에 접었다펴는 섹션으로 연결). 시드 데이터 추가로 기존 테스트 2개(`test_messaging.js`, `test_booking_approval.js`)가 "담당 고객 목록엔 이 테스트가 만든 건만 있다"고 가정한 부분이 깨져서, 특정 고객명으로 좁혀 찾도록(`hasText`) 고치고 건수 기대값(2→3)을 시드 1건을 반영해 수정했다. 9개 테스트 전부 통과. 브라우저로 직접 확인: 구매자 데모 로그인 → 신차 케어 서비스 진입 시 등록된 차량 없음 안내 없이 바로 "작업중" 주문이 보임, 카마스터 대리등록 폼에서 기존/신규 고객 문구가 전화번호 입력 즉시 바뀜, 제출 후 "승인 필요"는 늘지 않고 "고객 확인 대기"만 1건 늘어남을 확인.

## v6.39 (2026-08-25, 카마스터 평가하기 화면 — 실명 노출 잔여분 수정)

v6.37에서 카마스터 실명 노출을 전수 조사했지만 한 곳을 놓쳤다. `customer.js`의 다른 모든 카마스터 이름 표시는 `karmasterDisplayName()`을 거치는데, 평가하기 화면(`renderRating`)의 제목만 `Store.getKarmaster(targetId).name`을 직접 읽고 있었다 — 고객이 카마스터를 평가하러 들어가면 화면 제목에 실명이 그대로 노출됐다.

**변경**: 해당 한 줄을 `karmasterDisplayName(Store.getKarmaster(targetId))`로 고쳤다 — 시공업체 평가(대상이 실명 정책과 무관한 업체명)는 그대로 둔다.

**영향받은 파일**: `customer.js`(`renderRating` 1줄). 9개 테스트 전부 통과. `customer.js` 안의 다른 모든 `Store.getKarmaster(...)` 호출부를 grep으로 재확인해 이 한 곳 외에는 전부 `karmasterDisplayName()`을 거치고 있음을 확인했다.

## v6.38 (2026-08-25, 스텝바 — 진행중인 단계가 "도착 전"처럼 보이던 문제 수정)

`renderStatusStepperHTML`/`renderPreReleaseStepperHTML` 둘 다 각 단계로 "들어오는" 연결선의 채움 비율을 `i < idx`(현재 단계보다 앞선 단계만)로 계산하고 있었다. 그 결과 예를 들어 지금 커스터마이징(시공) 중이어도, 그 직전 연결선(탁송→시공)이 채워지지 않아 마치 아직 업체에 도착하지 않은 것처럼 보였다 — "출고---탁송  시공  탁송  도착"처럼 시공 단계가 끊겨 보이는 문제.

**변경**: 채움 조건을 `i <= idx`로 고쳐서, 현재 단계로 들어오는 선도 함께 채워지도록 했다("출고---탁송---시공  탁송  도착") — 그 단계에 이미 도달했다는 뜻이니 당연히 채워져야 한다. 여기에 더해, "이미 끝난 단계(초록, 정적)"와 "지금 진행중인 단계"를 구분하기 어렵다는 지적을 반영해 현재 단계의 점(dot)에 은은한 펄스 애니메이션(`dstep-pulse`, 1.6초 주기)을 추가했다 — 색만으로는 구분이 잘 안 되던 것을, 깜빡임으로 "지금 여기서 진행 중"임을 바로 알아볼 수 있게 했다.

**영향받은 파일**: `store.js`(두 스텝바 함수의 `fillPct` 계산식), `app.css`(`.dstep.cur .dstep-dot`에 펄스 애니메이션 추가). 9개 테스트 전부 통과(순수 시각 변경). 브라우저로 커스터마이징 단계에서 실제 DOM을 측정해 현재 단계까지의 연결선이 100% 채워지는 것, 현재 단계 점에 `dstep-pulse` 애니메이션(1.6s, 무한반복)이 걸려있는 것을 직접 확인했다.

## v6.37 (2026-08-25, 카마스터 실명 노출 — 신규 생성 시점 닉네임 누락 + 로그 메시지 누락 수정)

v6.19에서 만든 `karmasterDisplayName()`은 현대/기아 소속이 아닌 카마스터가 닉네임을 안 정했으면 실명으로 폴백하는 의도된 비대칭을 갖고 있는데, v6.30의 "비가입자 로그인" 재설계 이후로 새로 생기는 카마스터(승인 시점에 생성)는 전부 닉네임이 빈 채로 시작해서 이 폴백에 매번 걸렸다 — 브랜드가 현대/기아가 아닌 이상(이 세션의 테스트 데이터 대부분이 그랬다) 결과적으로 실명이 계속 노출됐다.

**신규 카마스터 자동 닉네임**: `approveContractAsUnregistered()`가 카마스터를 새로 만드는 시점에 `_autoNickname()`으로 기본 닉네임을 바로 지어준다 — 이름에서 "카마스터" 접미사를 떼고 "매니저"를 붙이는 씨드 데이터(도현매니저 등)와 같은 스타일이며, 겹치면 숫자를 붙여 피해간다. 이제 신규 카마스터도 처음부터 닉네임이 채워진 채로 시작해, 명시적으로 실명 표시를 선택하지 않는 한 실명이 새지 않는다.

**로그 메시지 누락 수정**: 같은 함수가 남기는 처리 이력 로그(`미가입 카마스터(${name})가 ...`)가 실명을 그대로 박아넣고 있었는데, 이 로그는 카마스터 본인 화면뿐 아니라 고객의 "전체 처리 이력"에도 그대로 노출된다 — 다른 로그 메시지들은 전부 "카마스터"로만 지칭하는데 이 한 곳만 예외였다. 이름을 빼고 다른 로그들과 같은 방식으로 통일했다.

**영향받은 파일**: `store.js`(`_autoNickname()` 신설, `approveContractAsUnregistered()`의 닉네임 생성·로그 메시지 수정). 9개 테스트 전부 통과(기존 테스트의 실명 부분 문자열 검증은 신규 닉네임에도 이름이 포함되므로 그대로 통과). 브라우저로 신규 미가입 카마스터 승인 직후 고객 화면에 실명이 아닌 자동 생성 닉네임만 보이는 것, "전체 처리 이력"에서도 실명이 빠진 것을 직접 확인했다.

## v6.36 (2026-08-25, 샘플 이미지 — 차량/문서 아이콘으로 실감 개선)

지금까지 "샘플 이미지 추가"가 만들어내던 이미지는 단색 배경에 반투명 원 하나와 텍스트뿐이라, 무엇을 찍은 건지 전혀 알아볼 수 없었다. 실제 사진 파일은 오프라인 데모(외부 리소스 로드 불가)에서 쓸 수 없어서, 라벨 문맥에 맞는 벡터 아이콘을 직접 그려 넣는 방향으로 개선했다.

**변경**: `generateSamplePhoto(index, label)`이 라벨 문자열을 보고 아이콘을 고른다 — "사업자등록증"이 포함되면 문서 아이콘(체크마크 포함), 그 외(현장사진/인수완료사진/검수사진 등 차량·작업 관련)는 차량 측면 실루엣 아이콘을 쓴다. 라벨에 "완료"나 "검수"가 들어있으면 차량 옆에 체크마크 배지를 추가로 얹어 "작업 중"과 "다 끝난 것"을 시각적으로 구분했다. 호출부(customer.js/karmaster.js/shop.js/admin.js 5곳)는 이미 라벨 문자열을 넘기고 있어 전혀 수정할 필요가 없었다 — `generateSamplePhoto` 함수 하나만 바꿔 전체 반영됐다.

**영향받은 파일**: `store.js`(`generateSamplePhoto` 재작성 1곳). 9개 테스트 전부 통과(순수 시각적 변경이라 로직 영향 없음). 브라우저로 커스터마이징 현장사진(차량 아이콘)·인수완료사진(차량+체크마크)·사업자등록증(문서+체크마크) 세 종류를 직접 렌더링해 확인했고, 실제 카마스터 화면의 사진 업로드 패널에서도 정상적으로 반영되는 것을 확인했다.

## v6.35 (2026-08-25, 배송 상세정보 — 위치·타이틀·게시 범위 재정리)

v6.34에서 "상세정보 입력 (선택)"을 CUSTOMIZING 카드 옆에 접이식으로 뺐는데, 두 가지 문제가 남아있었다. (1) 제목만 봐서는 이게 커스터마이징 얘기인지 배송 전반 얘기인지 구분이 안 갔다 — 실제 내용(기사정보·ETA·위치코멘트)은 커스터마이징이 아니라 배송/진행 상황 전반에 대한 것이었다. (2) [초안저장]/[게시하기] 버튼이 프로미넌트 카드(공정현황)와 접이식 카드 사이 어중간한 위치에 있어, 정확히 무엇을 게시하는지 UI만으로는 가늠할 수 없었다.

**변경**: 흩어져 있던 공정현황/지연사유와 기사정보/ETA/위치코멘트/메모를 다시 하나의 카드로 합쳤다 — "배송 상세정보 입력 (선택)"이라는 명확한 제목을 달고, [초안저장]/[게시하기] 버튼을 그 카드 안에 함께 둬서 "이 카드 전체를 게시한다"는 게 명확해지도록 했다. 위치도 CUSTOMIZING 카드 옆이 아니라 스텝 바 바로 아래로 옮겼다 — 기사정보·ETA·위치코멘트가 특정 단계가 아니라 이 예약의 진행상황 전반에 대한 정보이기 때문이다. 사진 업로드(현장사진/인수완료사진)는 이 카드에서 완전히 분리해 각 단계별 작업 카드(예: "② 지정업체에서 작업 중") 바로 아래 붙였다 — 게시 게이트 없이 즉시 반영되는 별개 메커니즘이라, 초안·게시 흐름과 섞이면 오히려 헷갈리기 때문이다.

**영향받은 파일**: `karmaster.js`(`renderAugmentationPanel()` 하나의 카드로 재통합, `renderCustomerDetail()`의 섹션 순서를 스텝바→상세정보→계약내용→진행상황으로 재배치, CUSTOMIZING/DELIVERED 단계별 사진 패널을 각 작업 카드로 이동, EXCEPTION 카드의 안내 문구를 새 위치/제목에 맞게 수정). 9개 테스트 전부 통과. 브라우저로 "배송 상세정보" 카드를 열어 기사정보·ETA·위치코멘트·공정현황·저장·게시 버튼이 전부 한 카드 안에 있는 것, 게시가 정상 동작하는 것, 사진 업로드가 CUSTOMIZING 작업 카드 바로 아래 붙어있는 것을 직접 확인했다.

## v6.34 (2026-08-25, 최종 수령지 기본값 · CUSTOMIZING/배송보강정보 UI 재구성 · 출고 전 단계 스텝바)

리뷰에서 나온 세 가지 UX 문제를 함께 고쳤다.

**최종 수령지 기본값**: 출고 요청 화면의 "최종 수령지" 입력이 매번 빈 칸이라 고객이 항상 타이핑해야 했다. 실제로는 `DEALERSHIP`(영업소 직행)뿐 아니라 `AFFILIATED_SHOP`(제휴 시공소 경유)도 시공 후 최종적으로는 영업소로 돌아온다는 게 현재 기준이라(추후 조정 가능), 이 두 경우엔 "영업소"를 기본값으로 미리 채워둔다. `CUSTOM_ADDRESS`(고객 지정 장소)만 매번 실제로 다르므로 빈 칸 그대로 둔다. 필드 자체는 항상 편집 가능해서 기본값이 있어도 고객이 원하면 언제든 직접 수정할 수 있다.

**CUSTOMIZING/배송보강정보 UI 재구성**: 지정업체 작업 중(CUSTOMIZING) 화면이 "완료 버튼만 있는 카드" → "기사정보·ETA·위치코멘트가 먼저 나오고 그 뒤에야 커스터마이징 공정현황이 나오는 배송보강정보 카드" → "별도로 떨어진 현장사진 카드", 이렇게 3단으로 흩어져 있었다. 게다가 기사정보/ETA/위치코멘트는 애초에 CUSTOMIZING 단계(차가 업체에 서 있는 중)엔 의미가 없는 필드였다. `renderAugmentationPanel()`을 재구성해 실제로 매번 채우는 것(공정현황 또는 지연사유 + 현장사진 + 초안저장/게시하기 버튼)은 항상 펼쳐진 채로 맨 위에 두고, 자주 안 쓰는 보조정보(기사 성명·연락처, ETA, 위치보정코멘트, 특이사항메모)는 "상세정보 입력 (선택)" 접이식(`<details>`, 기본 닫힘) 안으로 옮겼다. 이 구조는 IN_TRANSIT/DELIVERED/EXCEPTION 등 다른 단계에도 동일하게 적용된다.

**출고 전 단계 스텝바 신설**: 기존 배송(탁송) 스텝바(`renderStatusStepperHTML`)는 설계상 IN_TRANSIT 이후부터만 보여주고 계약확인~출고요청 단계는 아무 시각화가 없었다. `renderPreReleaseStepperHTML(r)`을 새로 만들어 "계약등록(고객요청) → 카마스터 확인(계약등록) → 출고요청(계약확정+출고요청)" 3단계를 같은 스타일(`.dstepper`)로 보여준다. 기존 배송 스텝바와는 완전히 분리된 별도 컴포넌트로, 하나로 이어붙이지 않았다 — IN_TRANSIT 이상으로 넘어가면 이 스텝바는 더 이상 표시되지 않는다(끝난 단계를 다음 화면까지 끌고 가지 않음).

**영향받은 파일**: `customer.js`(`renderReleaseBox` 기본값, `renderDetail`의 3개 뷰에 스텝바 삽입), `karmaster.js`(`renderAugmentationPanel` 재구성, `renderApprovalReview`/`renderCustomerDetail`/`renderUnregisteredApproveForm`에 스텝바 삽입), `store.js`(`renderPreReleaseStepperHTML`/`preReleasePhaseIndex`/`PRE_RELEASE_STEPS` 신설). 9개 테스트 전부 통과. 브라우저로 계약등록→카마스터승인→고객확인→출고요청까지 스텝바가 정확히 진행되는 것, 최종 수령지가 "영업소"로 자동 채워지는 것, CUSTOMIZING 단계에서 공정현황 입력→게시가 상세정보를 열지 않고도 끝까지 이어지는 것을 직접 확인했다.

## v6.33 (2026-08-25, 미등록 카마스터 안내 문구 — "전달됨"과 "조회번호 전달"을 분리)

등록된 카마스터 케이스는 "~님의 화면에 등록하신 계약 내용이 바로 전달되었습니다"라고 명확히 안심시키는데, 미등록 카마스터 케이스는 "조회번호를 직접 전달해 주세요, 앱이 대신 보내지 않습니다"만 있어 계약 정보 자체도 전달 안 된 것처럼 읽혔다. 실제로는 계약 내용은 등록 즉시 시스템에 저장되고, 카마스터가 못 보는 유일한 이유는 조회번호를 그 사람에게 전달하는 수동 단계 하나뿐이다.

**변경**: 미등록 카마스터 안내 문구를 "계약 내용은 이미 등록되어 카마스터가 접속하는 즉시 확인할 수 있습니다"로 시작하도록 재구성해, "정보 자체는 이미 전달·준비됨"과 "조회번호만 수동으로 전달 필요"를 분리했다. 실제 서비스에서는 이 조회번호도 SMS로 자동 발송될 예정이라는 안내를 덧붙였다 — 이 MVP는 실제 SMS 연동이 없어 수동 전달이 필요하지만, 그게 최종 설계는 아니라는 걸 명시했다.

**카마스터 등록 시점 확인**: 미등록 카마스터는 계약이 "등록"되는 시점이 아니라 실제로 그 계약을 "승인"하는 시점(전화번호+계약번호+조회번호로 본인확인 후)에 카마스터 레코드가 생성된다 — 그래서 카마스터 로그인 화면의 "데모 계정으로 빠른 로그인" 드롭다운에도 승인 전에는 나타나지 않고, 승인 직후부터 나타난다. 브라우저로 직접 확인: 계약 등록 직후엔 드롭다운에 없다가, `approveContractAsUnregistered` 승인 후에는 "알림테스트카마스터 · 010-6000-7000"처럼 바로 나타났다.

**영향받은 파일**: `customer.js`(미등록 카마스터 안내 문구 1곳). 9개 테스트 전부 통과(문구만 바뀌었을 뿐 로직 변경 없음). 브라우저로 문구 변경과 드롭다운 등장 시점을 직접 확인했다.

## v6.32 (2026-08-25, 카마스터 로그인 — 미가입자 시나리오 원클릭 체험 + 문구 정리)

v6.30에서 만든 "비가입자 로그인"(전화번호+계약번호+조회번호)은 실제로는 고객이 그 값을 카마스터에게 직접 전달해야 하는 구조라, 콘솔 접근 없이 화면만으로 시연/테스트하는 사람은 그 값을 알아낼 방법이 없었다 — "승인 필요"까지는 확인했지만, 그다음 미가입자 확인 절차를 실제로 밟아볼 수가 없다는 지적이었다. 시나리오를 문서로 안내하는 대신, 이미 있는 "빠른 로그인" 패턴을 그대로 확장해 화면 안에서 재현하기로 했다.

**신설**: `Store.getOrCreateUnregisteredDemo()` — 고정된 데모용 전화번호(010-9000-1234)로 아직 유효한(미완료·미가입) 예약이 있으면 재사용하고, 없으면 그 자리에서 새로 만들어 전화번호·계약번호·조회번호를 돌려준다. 카마스터 로그인 화면의 "데모 계정으로 빠른 로그인" 섹션에 "🧪 예시: 미가입 카마스터 시나리오 보기" 버튼을 추가해, 클릭 한 번으로 그 값을 채우고 곧바로 조회까지 실행해 승인 폼(또는 이미 승인된 상태라면 진행 상황 화면)으로 바로 들어간다. 반복 클릭해도 예약이 계속 쌓이지 않고 같은 건을 재사용한다 — 승인 전이면 승인 폼을, 승인 후(가입 전)라면 "가입하시겠어요?" 배너와 함께 진행 상황을 다시 보여준다.

**문구 정리**: 카마스터 로그인의 실제 비밀번호 입력란 placeholder를 "비밀번호 (정식 가입 시 발급된 비밀번호)"에서 그냥 "비밀번호"로 줄였다 — 가입 시 비밀번호를 만들고 기억하는 건 굳이 설명이 필요 없는 당연한 절차라는 지적을 반영했다. 4개 화면(카마스터/시공업체/배송기사/관리자)의 "테스트 계정으로 빠른 로그인" 라벨은 "데모 계정으로 빠른 로그인"으로 통일했다 — "샘플로 채우기(데모용)"과 같은 "데모" 용어로 맞춰, 실제 씨드 계정을 "테스트용"이라 부르던 것보다 자연스럽게 했다.

**영향받은 파일**: `store.js`(`getOrCreateUnregisteredDemo` 신설), `karmaster.js`(로그인 화면에 예시 버튼 추가, 비밀번호 placeholder 단순화, 라벨 변경), `admin.js`/`driver.js`/`shop.js`(라벨 변경). 9개 테스트 전부 통과(기존 로그인 흐름은 그대로라 무관). 브라우저로 예시 버튼을 반복 클릭해 같은 예약이 재사용되는 것, 승인 → "가입하시겠어요?" 배너까지 한 화면에서 이어지는 것을 직접 확인했다.

## v6.31 (2026-08-25, 데모/외부 시연용 "샘플로 채우기" 버튼)

외부 시연 시 필드 많은 폼(계약내역 등록, 관리자 계약 대리 등록)을 매번 손으로 채우는 게 번거롭다는 요청을 반영했다. 다만 진짜 데이터로 오해받을 위험이 있어, 버튼과 값 둘 다 "이건 샘플이다"가 한눈에 보이도록 설계했다.

**변경**: 두 폼 상단에 점선 테두리의 "🧪 샘플로 채우기 (데모용)" 버튼(새 `.btn-sample` 스타일, 주요 액션 버튼과 색부터 다르게)을 추가했다. 채워지는 값은 이름 "테스트고객", 전화번호 "010-0000-0000", 차량 "테스트카", 계약번호 "SAMPLE-XXXX"처럼 전부 한눈에 가짜임을 알 수 있게 했다 — 유일한 예외는 고객 폼의 카마스터 연락처로, 씨드 카마스터(김도현, 010-2222-3301)를 그대로 써서 클릭 한 번으로 승인까지 이어지는 전체 흐름을 바로 시연할 수 있게 했다.

**부수 발견**: 이 작업 중 관리자의 "계약 대리 등록" 폼(`renderNewContractForm`)이 재렌더링 시 입력값을 draft에서 다시 채워 넣지 않는 기존 버그를 발견해 함께 고쳤다 — 지금까지는 폼이 열려 있는 동안 다른 탭에서 Store가 바뀌어 `render()`가 다시 불리면 입력 필드가 화면상으로만 비어 보이는(내부 draft 값은 남아있는) 문제가 있었다. 샘플 채우기 버튼이 `render()`를 호출하면서 이 문제가 바로 드러나 발견했다.

**영향받은 파일**: `app.css`(`.btn-sample` 스타일 신설), `customer.js`(`renderRequest()` 버튼+핸들러), `admin.js`(`renderNewContractForm()` 버튼+핸들러, 입력값 재수화 버그 수정). 9개 테스트 전부 통과(테스트는 필드를 직접 채우므로 새 버튼과 무관). 브라우저로 두 폼 모두 샘플 채우기 클릭 시 제출 버튼이 즉시 활성화되는 것까지 확인했다.

## v6.30 (2026-08-25, 카마스터 비가입자 로그인 재설계 — 조회코드와 정식 비밀번호 분리)

v6.28에서 만든 "온보딩 시 확인코드가 그대로 로그인 비밀번호가 되는" 방식을 다시 설계했다. 확인코드는 계약 등록 시점에 카마스터에게 "전달되는" 값이라 이미 여러 사람이 알 수 있는 값인데, 이걸 그대로 영구 비밀번호로 쓰면 보안 의미가 약해진다는 지적을 반영해 — 확인코드는 "그 계약 1건에 대해서만, 신차인도서비스가 끝날 때까지만" 쓰는 임시 접근 수단으로 한정하고, 계속 활동할 뜻이 있을 때만 별도로 물어 완전히 새로운 정식 비밀번호를 발급하도록 분리했다.

**데이터 계층**: `Store.getReservationByPhoneContractCode({ phone, contractNumber, confirmCode })`를 신설했다 — 카마스터 전화번호+제조사 계약번호+조회번호 3개가 모두 일치해야 하고, 이미 정식 가입(비밀번호 보유)한 카마스터의 예약이거나 신차인도서비스가 완료(CONFIRMED)된 예약은 대상에서 제외한다. `getReservationByContractInfo`(계약번호+계약자명 복구 조회)는 승인 전후 상관없이 쓸 수 있도록 범위를 넓혔다(단, 미가입·미완료 건만). 기존 `onboardKarmasterAndFillContract`(가입+승인 한 번에)는 `approveContractAsUnregistered`(승인만, pin은 비워둠)로 대체했고, 새 `registerKarmasterPassword(karmasterId)`가 조회코드와 무관한 새 비밀번호를 발급한다. `getReservationByToken`(조회번호 단독 조회)은 더 이상 쓰이지 않아 제거했다.

**화면**: 카마스터 로그인 화면의 진입 버튼을 "비가입자이신가요? 계약 확인하기"로 바꿨다(가입을 전제하지 않는 문구). 새 "비가입자 로그인" 화면은 전화번호·제조사 계약번호·조회번호 3개를 함께 입력받고, 조회번호를 잊었다면 "계약번호·계약자명으로 찾기" 복구 화면에서 다시 확인할 수 있다. 본인확인이 끝나면 예약 1건만 보여주는 화면으로 들어간다 — 아직 승인 전이면 승인 폼을, 이미 승인됐다면 카마스터 대시보드와 동일한 진행 상황 화면(`renderCustomerDetail` 재사용, 메시지 주고받기 포함)을 그대로 보여준다. 승인 직후에는 "앞으로도 계속 활동하실 계획이신가요?" 배너로 가입 여부를 물을 뿐 강제하지 않으며, "가입하기"를 누르면 그 순간 새 비밀번호가 발급되고 일반 로그인 세션(전체 대시보드)으로 자연스럽게 전환된다.

**영향받은 파일**: `store.js`(조회·승인·가입 메서드 재설계), `karmaster.js`(온보딩 섹션 전면 재작성 — 비가입자 로그인/복구/단일예약뷰/가입안내 배너 신설), `admin.js`(관리자 대리 온보딩이 새 `approveContractAsUnregistered` 호출하도록 변경). `test_unregistered_karmaster_onboarding.js`를 새 3필드 로그인 흐름에 맞게 갱신했다. 9개 테스트 전부 통과. 브라우저로 전체 시나리오(승인 → 가입 배너 → 가입 → 실제 비밀번호로 재로그인 → 예전 조회코드는 더 이상 통하지 않음 확인, 그리고 별도로 복구 화면에서 조회번호 재확인)를 직접 실행해 검증했다.

## v6.29 (2026-08-25, UI 폴리시 — 비밀번호 필드 크기, 여백, 탭 전환 시 레이아웃 흔들림)

사용자 리뷰에서 지적된 화면 완성도 문제들을 모아 고쳤다.

**비밀번호 필드**: `app.css`의 `input[type=text], input[type=tel], input[type=date], select { width:100% }` 규칙에 `input[type=password]`가 빠져 있어 비밀번호 필드만 브라우저 기본 크기로 작게 나오던 문제를 고쳤고, `input:disabled`에 회색 배경/커서 스타일을 추가했다. 비활성 필드 옆의 별도 안내 문구("비밀번호 로그인은 추후 지원 예정입니다")는 없애고 `placeholder="비밀번호 (추후 지원 예정)"`로 필드 안에 흡수했다(구매자/시공업체/배송기사/관리자 4개 화면).

**여백**: 카마스터·시공업체·배송기사·관리자 대시보드의 로그아웃 버튼 줄, 관리자의 "+ 계약 대리 등록" 버튼 줄에 `margin-bottom`을 넣어 바로 아래 내용과 붙어 보이던 문제를 고쳤다.

**탭 전환 시 화면이 밀리는 문제**: 슈퍼바이저 화면에서 탭을 바꿀 때(특히 "통합 사용자" 활성화 시) 탭 줄 높이가 바뀌며 아래 내용 전체가 밀리던 버그를 재현해 원인을 찾았다 — `button.btn-primary { width:100% }`가 폼 하단 전체너비 CTA용으로 만든 규칙인데, 활성 탭 버튼도 `btn-primary` 클래스를 함께 쓰다 보니 그 버튼만 행 전체 너비로 늘어나 뒤따르는 버튼들이 다음 줄로 밀려나는 게 진짜 원인이었다. `button.btn-sm { width: auto }`를 명시해 탭처럼 여러 개가 한 줄에 놓이는 작은 버튼은 항상 내용 크기만큼만 차지하도록 고쳤다(부수적으로 `.btn { white-space: nowrap }`, `.btn-row { flex-wrap: wrap }`도 추가해 좁은 화면에서 텍스트가 버튼 내부에서 줄바꿈되는 대신 행 자체가 안전하게 줄바꿈되게 했다).

**구매자 화면**: "계약과 무관하게 카마스터 평판만 확인하기" 링크가 신차 케어 서비스 소속처럼 보이던 것을, 신차인도서비스 버튼 줄 바로 아래로 옮겼다.

**영향받은 파일**: `app.css`(비밀번호 필드폭, disabled 스타일, `.btn-sm` width 수정, nowrap/flex-wrap 추가), `customer.js`/`shop.js`/`driver.js`/`admin.js`(비밀번호 힌트 문구 정리, 로그아웃/버튼 여백), `karmaster.js`(로그아웃 여백), `customer.js`(카마스터 평판 링크 위치). 9개 테스트 전부 통과. 브라우저에서 비밀번호 필드 실측 크기(전화번호 필드와 동일 400×39px), 슈퍼바이저 4개 탭 전환 시 탭 줄 높이·본문 위치가 완전히 고정되는 것, 버튼-콘텐츠 간 16px 여백을 직접 측정해 확인했다.

## v6.28 (2026-08-25, 로그인 보안 개선 — 카마스터 실비밀번호 + 나머지 역할 비밀번호 필드 예고)

지금까지 모든 역할이 전화번호만으로 로그인됐다. 카마스터는 이 앱에서 유일하게 가입이 강제되지 않는 역할이라(user-account-role-model-spec.md) 전화번호만 알면 남의 계정에 들어갈 수 있다는 실질적인 보안 허점이었다. 다른 역할(구매자/시공업체/관리자/배송기사)은 가입이 강제되거나 관리자가 직접 만드는 계정이라 상대적으로 덜 급하지만, 화면상으로도 "언젠가 비밀번호가 생긴다"는 걸 미리 준비해두기로 했다.

**카마스터 — 실제 비밀번호 도입**: 카마스터 온보딩(`Store.onboardKarmasterAndFillContract`) 시점에 발급하던 임의 4자리 `pin`을, 이미 존재하던 확인코드(`r.confirmCode` — 조회번호 방식이든 v6.26의 계약정보 방식이든 온보딩에 쓰인 그 값)로 대체했다. 이 코드가 곧 그 카마스터의 로그인 비밀번호가 된다 — 조회번호 경로로 온보딩했다면 이미 알고 있는 값이고, 계약정보 경로로 온보딩했다면 온보딩 완료 직후 대시보드에 "로그인 비밀번호가 발급되었습니다: NNNNNN" 배너로 1회 안내한다(확인 후 닫을 수 있고, 로그아웃 시 사라진다). 로그인 화면에는 실제 `type="password"` 입력란을 추가했고, `Store.getKarmasterByPhone`으로 찾은 계정의 `pin`과 대조해 일치할 때만 로그인을 허용한다. 씨드 카마스터(k1/k2/k3)는 기존에 이미 갖고 있던 `pin`(1111/2222/3333, 이전엔 어디서도 쓰이지 않던 죽은 필드였다)을 그대로 초기 비밀번호로 쓴다. "테스트 계정으로 빠른 로그인" 드롭다운은 지금처럼 비밀번호 없이 그대로 뒀다 — 9개 Playwright 테스트 전부가 이 경로에 의존하고, 데모/QA 전용 우회 경로라는 성격 자체는 바뀌지 않았기 때문이다.

**나머지 역할 — 장식용 비밀번호 필드**: 구매자·시공업체·관리자(커뮤니티관리자/슈퍼바이저 공용)·배송기사 로그인 화면에 `disabled` 처리된 `type="password"` 입력란과 "비밀번호 로그인은 추후 지원 예정입니다" 안내 문구를 추가했다. 실제 검증 로직은 전혀 없다 — 화면만 앞으로의 실제 인증을 미리 준비해두는 순수 UI 변경이다.

**영향받은 파일**: `store.js`(pin 발급 로직 1곳), `karmaster.js`(로그인 화면 실비밀번호 검증, 온보딩 배너), `customer.js`/`shop.js`/`driver.js`/`admin.js`(로그인 화면에 비활성 비밀번호 필드 추가). 카마스터 온보딩 버튼 문구가 "조회번호로 첫 계약 등록하기"에서 "첫 계약 등록하기"로 바뀌어(v6.26에서 두 조회 방식이 생겼으므로) `test_unregistered_karmaster_onboarding.js`의 셀렉터를 갱신했다. 9개 테스트 전부 통과. 브라우저로 카마스터 로그인 화면에서 틀린 비밀번호 거부 → 올바른 비밀번호(1111) 로그인 성공, 그리고 신규 온보딩 시 비밀번호 발급 배너가 뜨는 것까지 직접 확인했다.

## v6.27 (2026-08-25, 카마스터 대시보드 검색에 제조사 계약번호 추가)

카마스터 대시보드의 담당 고객 검색이 접수번호(`r.id`)·고객이름·연락처만 대상으로 했다. v6.25에서 추가한 제조사 계약번호도 실제로 고객을 찾을 때 쓰는 정보라, 검색 대상에 넣지 않으면 새로 추가한 필드가 검색에서는 무용지물이었다.

**변경**: 카마스터 대시보드의 `#km-search` 필터에 `r.contractNumber` 매칭을 추가하고, 플레이스홀더를 "접수번호·제조사 계약번호·고객이름·연락처로 검색"으로 갱신했다.

**영향받은 파일**: `karmaster.js` 한 곳(`renderDashboard()`의 검색 필터). 9개 테스트 전부 통과(검색 로직에 항목 추가만 했을 뿐 기존 매칭 조건은 그대로라 무관). 브라우저로 제조사 계약번호("ABC-9999")로 검색해 해당 고객이 바로 필터링되는 것을 확인했다.

## v6.26 (2026-08-25, 미가입 카마스터 조회 방식 이원화 — 조회번호 + 계약정보)

`user-account-role-model-spec.md`는 카마스터를 고객과 달리 가입이 강제되지 않는 역할로 규정한다 — 고객이 계약을 등록하면 카마스터 전화번호로 자동 전달되고, 카마스터는 언제든 그걸 찾아 확인할 수 있어야 한다. 지금까지는 그 방법이 고객에게 직접 전달받은 6자리 조회번호(confirmCode) 하나뿐이었는데, 그 번호를 분실하거나 애초에 전달받지 못한 경우 대안이 없었다. v6.25에서 추가한 제조사 계약번호 덕분에, 계약서 자체의 정보(제조사 계약번호+계약자명)로도 찾을 수 있는 두 번째 경로를 열 수 있게 됐다.

**데이터 계층**: `Store.getReservationByToken`(기존, 그대로 유지) 옆에 `Store.getReservationByContractInfo({ contractNumber, customerName, carBrand })`를 신설했다. 계약번호+계약자명은 필수, 브랜드는 한 번 더 좁히는 선택적 확인용이다 — 두 값 모두 실제 계약서를 손에 쥔 사람만 알 수 있는 조합이라, 조회번호 방식과 동일한 수준의 자연스러운 인증 역할을 한다.

**화면**: 카마스터 온보딩 화면("첫 계약 등록 — 본인 확인")에 "조회번호로 찾기"/"계약번호로 찾기" 두 탭을 추가했다. 조회번호 탭은 기존 그대로고, 계약번호 탭은 제조사 계약번호·계약자명 입력란과 제조사(선택) 드롭다운을 보여준다. 어느 탭으로 찾았든 이후 흐름(내용 검토 → 본인 이름·소속 그룹·목적지 유형 입력 → 가입과 동시에 승인)은 완전히 동일한 코드를 공유한다 — 찾는 방법만 다를 뿐 승인 로직은 하나다.

**영향받은 파일**: `store.js`(조회 메서드 신설 1개), `karmaster.js`(`renderOnboard()` 전면 재구성 — 모드 토글 추가, 조회 로직을 모드별로 분기, 결과 이후 폼은 그대로 재사용). 관리자 화면은 이미 미배정 예약을 목록에서 직접 볼 수 있어(슈퍼바이저 한정) 별도 조회 UI가 필요 없다고 판단해 손대지 않았다. 9개 테스트 전부 통과(기존 테스트는 기본값인 조회번호 탭을 그대로 쓰므로 무관). 브라우저로 새 계약번호 탭으로 실제 미등록 카마스터를 찾아 가입·승인까지 완료되는 것을 직접 확인했다.

## v6.25 (2026-08-25, 제조사 계약번호 — 신차인도서비스 핵심 식별 필드 추가)

`new-car-delivery-tracking-service-spec.md` 2.1절은 "브랜드명+계약번호+계약자명" 3종을 계약을 특정하는 식별 키로 규정하고, 향후 Layer 1(제조사 실주행 트래킹) 연동에도 이 조합을 쓴다. 그런데 이 앱은 지금까지 자체적으로 채번하는 접수번호(`10-YYYYMM-NNNN` 형식, `r.id`)만 갖고 있었고, 제조사·딜러가 실제로 발급한 계약번호는 아예 입력받지 않았다 — 동명이인 고객을 구분할 방법이 없었고, 나중에 실제 제조사 시스템과 연동할 연결고리도 없는 상태였다.

**데이터 모델**: 예약에 `contractNumber`(문자열) 필드를 신설했다. `Store.startContractRequest`(고객 셀프 등록)와 `Store.createContractRecordDirect`(관리자 대리 등록) 양쪽 모두 이 필드를 받는다 — 이 앱이 자동 채번하는 접수번호(`r.id`)와는 완전히 별개 값이라는 점을 필드명과 주석에 명시했다.

**입력 폼**: 고객의 "계약내역 등록" 폼 최상단에 "핵심 식별 정보" 섹션을 신설해 제조사(브랜드)·제조사 계약번호·계약자명(내 이름) 3개를 한데 묶고, 세 필드 모두 필수값으로 검증한다(이전엔 브랜드도 선택사항이었다). 관리자의 "계약 대리 등록" 폼에도 동일하게 제조사 계약번호 입력을 추가하고 필수 검증했다.

**노출 지점**: 카마스터 승인 화면(신규/기가입 양쪽), 카마스터 대시보드의 진행중 계약 상세, 고객의 "계약 내용 확인"·"계약이 확정되었습니다" 화면, 관리자 상세 테이블과 대리처리 폼, 고객의 "내 계약 확인/이력 조회" 테이블(새 "제조사 계약번호" 컬럼 추가, 필터 검색 대상에도 포함) — 계약/차량 정보가 보이는 곳마다 `${carBrand} · ${contractNumber}` 형태로 함께 노출했다. 이력 조회 테이블의 기존 "계약번호" 컬럼은 사실 앱 내부 접수번호(`r.id`)였던 걸 "접수번호"로 이름을 바로잡고, 그 옆에 진짜 "제조사 계약번호" 컬럼을 별도로 추가했다 — 두 값의 성격이 다르다는 걸 명확히 하기 위함이다.

**영향받은 파일**: `store.js`(필드 신설 2곳), `customer.js`(등록 폼 재구성+검증, 계약확정/이력조회 화면 4곳), `karmaster.js`(승인/상세 화면 3곳), `admin.js`(대리등록 폼+상세+대리처리 폼 4곳). 테스트는 새 필수 필드 때문에 8개 시나리오 테스트 전부에 브랜드 선택+계약번호 입력을 추가해야 했다(관리자 대리등록 테스트 포함) — 그 과정에서 `test_unregistered_karmaster_onboarding.js`가 한 번 깨졌는데, 브랜드를 "기아"로 넣었더니 v6.19의 카마스터 실명 마스킹 정책(현대/기아 소속이면 닉네임 강제)이 새로 가입하는 카마스터에게 발동해 그 테스트가 검증하려던 "고객 화면에 카마스터 실명이 반영됨" 단언과 충돌했다 — 그 테스트만 브랜드를 "기타"로 바꿔 해결했다(정책은 그대로 두고, 그 정책과 무관한 걸 검증하는 테스트가 우연히 정책 조건에 걸리지 않게만 피함). 9개 테스트 전부 통과. 브라우저로 고객 등록 → 카마스터 승인 화면까지 실제로 계약번호("현대 · HD-2026-9999")가 그대로 이어지는 것을 직접 확인했다.

## v6.24 (2026-08-25, 카마스터 씨드 데이터에 닉네임 채우기)

v6.19에서 만든 `karmasterDisplayName()`은 이미 `nameDisplayMode: 'nickname'`을 기본값으로 두고 있었지만, 씨드 카마스터 3명(k1/k2/k3)의 `nickname` 필드가 빈 문자열이라 로직이 실명으로 폴백해 "카마스터 찾기" 화면에 여전히 실명이 노출되고 있었다 — 로직 버그가 아니라 데이터 누락이었다.

**변경**: `DEFAULT_KARMASTERS`의 k1/k2/k3에 각각 예시 닉네임(도현매니저/서연카마스터/준호쓰카)을 채웠다. 코드 변경 없이 데이터만 채운 것이라 다른 로직에는 영향이 없다.

**영향받은 파일**: `store.js` 한 곳(`DEFAULT_KARMASTERS` 배열). 9개 테스트 전부 통과(테스트는 실명을 텍스트로 검증하지 않고 연락처로 로그인/조회하므로 무관). 브라우저에서 localStorage를 초기화한 뒤 "카마스터 찾기" 화면을 다시 열어 실명 대신 닉네임이 표시되는 것을 직접 확인했다.

## v6.23 (2026-08-25, 고객 랜딩 — 카마스터 찾기 위계 조정)

신차인도서비스의 메인 동작은 "계약내역 등록"과 "계약 확인/이력조회" 둘이고, "카마스터 찾기"는 계약과 무관하게 평판을 미리 살펴보고 싶을 때 쓰는 부차 기능이다. 그런데 랜딩 화면 상단 버튼줄에 세 개가 동등한 무게로 나란히 있어 카마스터 찾기가 메인 기능처럼 보였다.

**변경**: 랜딩 상단 버튼줄에서 "카마스터 찾기"를 빼고 "계약내역 등록하기"/"내 계약 확인·이력조회" 두 개만 남겼다. 대신 (1) 랜딩 화면 맨 아래에 "계약과 무관하게 카마스터 평판만 확인하기 →" 작은 링크를 추가하고, (2) 계약내역 등록 폼의 "카마스터 연락처" 입력란 바로 아래에도 "연락처를 모르거나 카마스터를 아직 못 정했다면 → 카마스터 찾기" 링크를 넣어, 등록 도중에도 바로 리서치 화면으로 갈 수 있게 했다. 두 링크 모두 기존 `goto('karmasters')` 라우팅을 그대로 재사용한다.

**영향받은 파일**: `customer.js`(`renderLanding()`, `renderRequest()` 두 곳만 수정, 라우팅·로직 변경 없음). 기존 테스트는 "카마스터 찾기" 버튼을 클릭하는 흐름이 없어 무관 — 9개 테스트 전부 통과. 브라우저로 랜딩 하단 링크와 등록 폼 내 링크 둘 다 렌더링 확인했다.

## v6.22 (2026-08-25, 관리자 화면 완전 분리 — admin.html(커뮤니티관리자) / supervisor.html(슈퍼바이저))

v6.20에서 관리자를 슈퍼바이저/커뮤니티관리자 두 스코프로 나눴지만, 로그인 화면 하나(admin.html)에서 두 스코프 계정을 모두 고를 수 있어 여전히 "관리자"라는 하나의 화면처럼 보였다. 두 역할은 성격 자체가 다르다는 지적(슈퍼바이저=시스템관리자, 커뮤니티관리자=고객관리자)을 반영해 진입점부터 갈랐다.

**공유 스크립트 + 모드 플래그**: `admin.js` 하나를 `admin.html`과 신설 `supervisor.html`이 함께 로드하되, 각 HTML이 `admin.js`를 불러오기 전에 `window.ADMIN_MODE`를 `'community'`/`'super'`로 지정해둔다. `renderLogin()`이 이 값으로 `Store.getAdmins()`를 미리 필터링해 빠른 로그인 드롭다운에 해당 스코프 계정만 보여주고, 전화번호 로그인도 `admin.adminScope !== ADMIN_MODE`면 거부한다 — 같은 코드베이스를 두 벌로 복제하지 않으면서 진입 자체를 스코프별로 막았다. 로그인 이후의 탭 구성(`isSuper` 분기)은 기존 로직 그대로라 변경 없음.

**화면 문구**: `admin.html`은 "커뮤니티관리자 화면"으로 제목·네브바·footer-note를 다시 썼다. `supervisor.html`은 신설 화면으로, 기존 admin.html의 "관리자는 모든 단계를 대신 처리할 수 있어 데모 시연 시 신속하게 진행할 수 있습니다" 문구를 그대로 물려받았다 — 전 그룹에 걸친 대리처리 자체가 슈퍼바이저(전체 권한)의 성격이지 배정된 그룹 안에서 일하는 커뮤니티관리자의 것이 아니기 때문이다.

**index.html 진입 방식 차등화**: "내부 테스트용 바로가기"에서 커뮤니티관리자는 "👥 커뮤니티관리자 화면"이라는 이름 있는 링크로 유지하고, 슈퍼바이저는 이름 라벨 없이 `⚙` 아이콘 하나만(opacity 0.45, 줄 오른쪽 끝으로 분리 배치) 노출해 아무나 무심코 클릭하지 않도록 했다 — "시스템 콘솔"에 가깝게 취급해 달라는 요청 반영.

**영향받은 파일**: `admin.js`(ADMIN_MODE 플래그 도입, `renderLogin()` 스코프 필터링), `admin.html`(커뮤니티관리자 전용으로 문구 재작성), `supervisor.html`(신설), `index.html`(바로가기 두 링크로 분리). `tests/test_admin_fasttrack.js`는 원래 `admin_super` 계정으로 전 그룹 대리처리를 검증하던 테스트라, 로그인 대상을 `admin.html`에서 `supervisor.html`로 바꿨다(계정 id·빠른 로그인 동작은 그대로라 셀렉터 변경 없음). 다른 8개 테스트는 admin.html/supervisor.html을 전혀 쓰지 않아 무관. 9개 테스트 전부 통과 확인.

## v6.21 (2026-08-25, 랜딩 레이아웃 — 구매자 화면 단독 강조)

첫 화면(index.html)의 역할 카드 배치에서 시공업체 카드가 2행 전체(`grid-column:1/-1`)를 차지해 시각적으로 "메인 화면"처럼 보이는 문제가 있었다 — 이 서비스의 주 고객은 구매자인데 레이아웃이 그 우선순위를 반영하지 못하고 있었다.

**변경**: 구매자 화면 카드를 1행 단독으로 분리해 강조(테두리 2px, 더 큰 패딩/폰트)하고, 카마스터·시공업체는 그 아래 2열로 대등하게 배치했다 — 구매자를 지원하는 파트너/제공자라는 위계를 그대로 시각화했다.

**영향받은 파일**: `index.html`만 수정(순수 레이아웃 변경, 로직 없음). 기존 테스트는 모두 각 role html로 직접 `goto()`하고 index.html의 클릭 흐름을 검증하지 않아 무관 — 9개 테스트 전부 통과 확인.

## v6.20 (2026-08-25, 슈퍼바이저/커뮤니티관리자 분리 + Group 기반 스코프 필터링)

`user-account-role-model-spec.md` 1.3/4.5/7장 — 관리자를 단일 하드코딩 계정에서 슈퍼바이저(전체 권한)와 커뮤니티관리자(배정된 Group으로 범위 제한)로 나눴다. "오더의 스코프는 그 오더를 처리하는 카마스터/시공업체가 어느 Group에 속해 있는가로 정해진다"는 1.3절 원칙을 그대로 구현했다.

**데이터 모델**: 최상위 `admins[]`를 신설하고 슈퍼바이저 1명 + 울산 그룹 담당 커뮤니티관리자 1명을 시드로 넣었다(`adminScope`: `'super'`/`'community'`, `assignedGroupIds[]`). `getAdmins`/`getAdmin`/`getAdminByPhone`을 추가했다. 스코프 판정은 예약·케어주문·업체 각각에 대해 `reservationInAdminScope`/`careOrderInAdminScope`/`shopInAdminScope`로 구현했다 — 슈퍼바이저는 항상 통과, 커뮤니티관리자는 담당 카마스터/시공업체의 `groupIds`가 `assignedGroupIds`와 하나라도 겹쳐야 통과한다. 아직 카마스터가 배정되지 않은 건(고객요청 단계에서 미등록 카마스터 지정)은 스코프가 정해지지 않아 커뮤니티관리자에게는 보이지 않는다.

**로그인**: 카드 클릭 한 번으로 끝나던 관리자 로그인을 다른 역할과 동일한 전화번호 로그인(+빠른 로그인 드롭다운)으로 바꿨다 — 관리자는 셀프 온보딩 대상이 아니므로(1.6절) 카마스터/시공업체처럼 "신규 등록" 경로는 없고, 슈퍼바이저가 미리 만들어둔 계정으로만 들어온다는 원칙을 반영했다.

**화면**: 헤더에 로그인한 관리자의 이름과 스코프(전체 권한 / 담당 그룹명)를 표시한다. 신차인도서비스·신차 케어 서비스·업체 승인 탭의 목록이 커뮤니티관리자에게는 스코프 필터링된 채로 보인다. "통합 사용자" 탭은 여러 그룹의 고객 정보가 한데 섞여 보이는 화면이라, 커뮤니티관리자에게는 탭 자체를 숨겼다(배정되지 않은 Group의 고객정보 열람을 허용하지 않는다는 원칙, 1.3절) — 세션에 남아있던 탭 상태가 `users`였더라도 커뮤니티관리자로 로그인하면 안전하게 `delivery` 탭으로 대체된다.

**영향받은 파일**: `store.js`(`admins[]`+접근자 3개+스코프 판정 함수 3개 신설), `admin.js`(로그인 전면 재작성, 탭 목록·목록 필터링·헤더 표시 갱신). 테스트 1개(`test_admin_fasttrack.js`)의 카드 클릭 로그인을 슈퍼바이저 빠른 로그인으로 갱신(이 테스트는 전 그룹에 걸친 신속처리를 검증하는 목적이라 스코프 제한이 없는 계정이 맞다). 기존 테스트 9개 모두 통과. 브라우저로 울산 스코프 밖 카마스터(경기 그룹)의 계약을 만들어 커뮤니티관리자에게는 안 보이고 슈퍼바이저에게만 보이는 것, "통합 사용자" 탭이 커뮤니티관리자 화면에서 사라지는 것까지 직접 확인했다.

**의도적으로 미룬 부분**: 개인 계정의 역할 속성 부착/해제(계정 생성·정지·삭제)를 슈퍼바이저만 할 수 있게 강제하는 UI는 아직 없다(애초에 계정 정지/삭제 기능 자체가 이 MVP에 없음). 여러 Group에 걸친 시공업체 등록 신청을 "한 곳만 승인해도 충분"하게 처리하는 것이나 슈퍼바이저의 사후 재검토·뒤집기 권한도 구현하지 않았다 — 지금은 승인 목록 자체가 스코프로 걸러지므로 커뮤니티관리자가 자기 그룹 것만 보고 승인하는 정도로 단순화했다. 관리자 대리 처리 액션(예: 강제 확정)이 서버 측에서 스코프를 강제하지는 않는다 — UI 목록에 안 보이는 건을 선택할 방법 자체가 없다는 것으로 사실상의 경계를 대신한다(클라이언트 전용 데모라는 이 프로토타입 전반의 한계와 동일선상).

## v6.19 (2026-08-25, 카마스터 닉네임/실명 표시 정책 + brandsHandled 자동 유추)

`user-account-role-model-spec.md` 3장(닉네임 전역 유일성)·4.2절(brandsHandled/brandAffiliation/nameDisplayMode)·5장(카마스터 실명 비노출)을 반영했다. 지금까지는 카마스터의 `name`(실명)이 모든 고객 화면에 그대로 노출되고 있었다 — 현대/기아 소속 카마스터의 개별 영업행위로 비칠 수 있는 실명 노출을 금지한다는 제조사 정책이 전혀 구현돼 있지 않았다.

**브랜드 취급 이력**: 예약에 `carBrand`(현대/기아/기타, 고객이 계약내역 등록 시 선택)를 추가했다. `brandsHandledFor(karmasterId)`가 그 카마스터가 담당한 예약들의 `carBrand`를 모아 매번 계산하고(별도 필드로 저장하지 않음 — v1.7 "자동 유추가 기본값" 원칙), `brandAffiliationFor(karmasterId)`가 여기서 `hyundai_kia`/`other`/`none`을 파생시킨다.

**표시명 결정 로직**: `karmasterDisplayName(k)`가 고객 화면에 보여줄 이름을 계산한다 — `hyundai_kia`면 `nameDisplayMode`를 무시하고 닉네임을 강제하며, 아직 닉네임이 없으면(이 MVP의 온보딩 폼이 닉네임을 필수로 받지 않으므로) 실명이 새지 않도록 마스킹된 전화번호로 대체한다. 반면 `hyundai_kia`가 아닌 카마스터가 그냥 닉네임을 안 정한 경우는 보호할 정책이 없으므로 실명을 그대로 보여준다 — 이 비대칭이 핵심이다(처음엔 무조건 마스킹으로 구현했다가, 테스트 3개가 깨지면서 모든 카마스터가 닉네임 없이는 전화번호로만 보이는 회귀를 잡아 고쳤다).

**카마스터 화면**: 대시보드 하단에 접이식 "표시 이름 설정" 패널을 추가했다 — 닉네임 입력 시 디바운스(300ms)로 실시간 중복 확인을 하고, 중복이면 숫자 접미사를 붙인 대안 후보 최대 3개를 자동 제안한다(3장 UX 그대로). `hyundai_kia` 소속이면 표시 방식(닉네임/실명) 토글 자체를 숨기고 강제 안내 문구만 보여준다. 닉네임 전역 유일성은 이 데모에서는 카마스터끼리만 비교한다(User.nickname과의 완전한 통합은 범위 밖).

**고객 화면**: 카마스터 검색 카드, 계약내역 등록 폼의 실시간 힌트, 계약 승인/확정 화면, 인도완료 화면, 메시지 패널 상대방 라벨, "내 계약 확인" 이력 검색·표기까지 — 카마스터 실명이 노출되던 8곳 모두 `karmasterDisplayName()`으로 교체했다. 관리자·카마스터 자신의 화면은 내부 전용이라 실명을 그대로 유지한다(문서의 명시적 예외).

**영향받은 파일**: `store.js`(carBrand 필드, `brandsHandledFor`/`brandAffiliationFor`/`karmasterDisplayName`/`isKarmasterNicknameTaken`/`setKarmasterProfile` 신설), `customer.js`(carBrand 선택 UI, 카마스터 실명 노출 8곳 교체), `karmaster.js`(표시 이름 설정 패널 신설), `admin.js`(계약 대리 등록 폼에 carBrand 추가). 기존 테스트 9개 모두 통과(디버깅 과정에서 실제로 3개가 깨졌다가, 위 마스킹 비대칭 수정과 `.admin-controls` 선택자 충돌 회피로 다시 통과시켰다). 브라우저로 브랜드 취급 이력 생성 → hyundai_kia 판정 → 마스킹 표시 → 닉네임 저장 → 표시 전환, 그리고 중복 닉네임 시 대안 후보 제안까지 전체 시나리오를 직접 실행해 검증했다.

**의도적으로 미룬 부분**: `brandsHandled[]`의 수동 오버라이드(문서는 "자동 유추가 기본값, 필요시 본인 수정 가능"이라 하지만 수정 UI는 만들지 않음), User.nickname과의 전역(전 역할 통합) 유일성, 시공업체·구매자 쪽 닉네임/표시명 체계(이 정책은 카마스터에 한정된 제조사 정책이라 다른 역할에는 애초에 적용 대상이 아님).

## v6.18 (2026-08-25, UnclaimedKarmasterProfile — 미가입 카마스터 평판 레코드 분리)

`user-account-role-model-spec.md` 4.2절의 `UnclaimedKarmasterProfile`을 도입했다. 고객이 아직 이 서비스에 등록되지 않은 카마스터의 전화번호로 계약을 등록하면, 그 카마스터가 실제로 가입하기 전부터 전화번호를 키로 하는 평판 레코드가 쌓이기 시작한다는 원칙이다 — `User`/`roleAttribute`와 같은 테이블에 합치지 않고 완전히 분리해서 관리한다(가입 시점에 phone 유일성 제약이나 가입일 같은 의미가 꼬이는 걸 피하기 위해).

**데이터 모델**: 최상위 `unclaimedKarmasters[]`를 신설했다. `Store.touchUnclaimedKarmaster(phone)`이 미등록 전화번호로 계약이 들어올 때마다 없으면 생성(있으면 그대로 유지)하고, `getUnclaimedKarmasterByPhone`/`getUnclaimedKarmasters()`(아직 연동 안 된 것만)를 추가했다. `maskPhone(phone)`로 "010-1234-\*\*56" 형태의 마스킹 표시 문자열을 만든다 — 4.2절이 "가입 전 표시 이름" 문제를 해결한 방식 그대로다(별도 이름 필드를 두지 않고 전화번호를 렌더링 시점에만 마스킹).

**연결 지점**: `startContractRequest`가 미등록 카마스터 전화번호를 받을 때마다 평판 레코드를 생성한다. `onboardKarmasterAndFillContract`(초대·자발적 가입 두 경로 모두 여기로 합류)가 실제로 카마스터를 가입시킬 때, 같은 전화번호의 미연동 레코드를 찾아 `linkedUserId`/`linkedAt`을 채운다 — 레코드는 지우지 않고 이력으로 남긴다. 평점 마이그레이션 로직도 함께 넣었지만(레코드에 `reviews > 0`이면 새 카마스터 레코드로 복사), 이 MVP 구조상 평가는 항상 카마스터가 이미 가입한 뒤에만 발생할 수 있어 실질적으로는 트리거되지 않는 코드다 — 문서의 설계를 정확히 따르되 이 한계를 솔직히 남겨둔다.

**고객 화면**: "카마스터 찾기" 검색 화면 하단에 "미가입 카마스터 (평판만 확인 가능)" 섹션을 신설해, 마스킹된 전화번호와 평점(있으면)을 보여준다 — 아직 가입하지 않은 카마스터도 다른 고객이 이미 계약을 진행 중이라면 평판이 쌓이고 있다는 걸 보여주는 화면이다. 브라우저로 "미등록 전화번호로 계약 등록 → 검색화면에 마스킹 표시 → 온보딩 → linkedUserId 연결 확인"까지 전체 시나리오를 직접 실행해 검증했다.

**영향받은 파일**: `store.js`(`unclaimedKarmasters[]` 신설, 관련 메서드 4개, 두 함수에 연결 지점 삽입), `customer.js`(카마스터 검색 화면에 미가입 섹션 추가). 순수 추가 기능이라 기존 테스트 9개 모두 수정 없이 통과.

## v6.17 (2026-08-25, Shop 독립 레지스트리 + 신규 업체 등록·승인 워크플로)

`user-account-role-model-spec.md` 4.3절 — 시공업체를 "카마스터처럼 개인 역할 속성에서 파생되는 게 아니라, 처음부터 독립적으로 존재하는 최상위 엔티티(레지스트리)"로 재정의하는 작업을 반영했다. 지금까지는 `DEFAULT_SHOPS` 3개가 전부였고 신규 등록·심사 절차 자체가 없었다.

**데이터 모델**: `Shop`에 `ownerUserId`, `groupIds[]`, `verificationStatus`(`pending`/`approved`/`rejected`), `businessRegistrationNumber`, `businessRepresentativeName`, `businessStartDate`, `businessVerifiedAt`, `businessRegistrationDocUrl` 필드를 추가했다. 기존 시드 3개 업체는 이 필드가 생기기 전부터 있던 데모 데이터라 소급 심사 없이 `approved`로 간주한다. `Store.registerShop(...)`이 국세청 "사업자등록정보 진위확인" API를 흉내낸다 — 실제 API 호출은 이 환경에서 불가능하므로, **사업자등록번호 10자리 형식 + 대표자성명·개업일자 필수 입력**만으로 통과/실패를 시뮬레이션하고, 실패 문구에 "데모 시뮬레이션"임을 명시했다. 같은 사업자등록번호가 이미 `pending`/`approved` 상태면 중복 등록을 차단한다. `approveShop`/`rejectShop`, 그리고 고객에게는 항상 승인된 업체만 보여줘야 한다는 원칙을 강제하는 `getApprovedShops()`를 추가했다.

**시공업체 화면**: 로그인 화면에 "아직 계정이 없으신가요? 신규 업체 등록하기"를 추가해(카마스터 온보딩과 동일한 패턴) 업체명·전화번호·사업자등록번호·대표자성명·개업일자·사업자등록증 이미지(샘플/파일 업로드)·소속 그룹(복수 선택)을 입력받는다. 등록 성공 시 `pending` 상태로 즉시 자동 로그인되지만, 대시보드 대신 "승인 대기 중" 화면만 보인다 — 승인이 나야 정상 대시보드로 전환된다. 반려 시에도 별도 안내 화면을 보여준다.

**고객·관리자 화면**: 고객의 신차 케어 서비스 시공사 선택 목록(`renderAmSetup`)과 관리자의 대리 신청 폼이 `getApprovedShops()`만 사용하도록 바꿔, 미승인 업체가 고객 앞에 노출되지 않는다는 원칙을 실제로 강제했다. 관리자에 4번째 탭 "업체 승인"을 신설해 대기 중인 업체를 사업자등록증 이미지·기본정보와 함께 보여주고 승인/반려 버튼을 제공한다(대기 건수를 탭 라벨에 배지로 표시). 이 데모에서는 슈퍼바이저/커뮤니티관리자가 아직 분리되지 않아 관리자 계정이 그 역할을 겸한다.

**영향받은 파일**: `store.js`(Shop 필드 확장, `registerShop`/`approveShop`/`rejectShop`/`getApprovedShops` 신설), `shop.js`(등록 폼·대기/반려 화면 신설), `admin.js`(업체 승인 탭 신설, 대리 신청 폼이 승인된 업체만 사용하도록 수정), `customer.js`(시공사 선택 목록이 승인된 업체만 사용하도록 수정). 순수 추가+가시성 필터링이라 기존 테스트 9개 모두 수정 없이 통과. 브라우저로 신규 업체 등록 → 승인 대기(고객 목록에서 제외 확인) → 관리자 승인 → 정상 대시보드 전환까지 전체 시나리오를 직접 실행해 검증했다.

**의도적으로 미룬 부분**: ShopStaff(직원이 이미 승인된 업체에 합류하는 흐름, owner의 초대/제외 관리)는 이번 슬라이스에 포함하지 않았다 — owner 단독 등록·로그인만 구현했다. 여러 Group에 걸친 신청의 "한 곳만 승인해도 충분" 규칙이나 슈퍼바이저의 사후 재검토·뒤집기 권한도 구현하지 않았다(현재는 단일 관리자 계정이 모든 그룹의 승인권을 사실상 가짐). "가입자 신원과 사업자 신원의 관계"(대표 본인 확인, PG사 정산 계좌 대조 등 최종 방어선)도 이 프로토타입 범위 밖이다.

## v6.16 (2026-08-25, Group(그룹/커뮤니티) 엔티티 도입 — 카마스터 region → groupIds 전환)

`user-account-role-model-spec.md` 1.3절의 `Group` 엔티티를 도입했다. v1.1까지의 "지역관리자" 개념이 v1.2에서 "커뮤니티관리자"로 바뀐 배경 그대로, 카마스터의 활동 범위를 문자열 하나(`region: '부산/울산'`)가 아니라 **여러 그룹에 복수 소속**하는 구조로 바꿨다.

**데이터 모델**: `store.js`에 최상위 `groups[]`를 신설하고 seed 카탈로그 5개(울산/부산/경기 — `region`, 수입차 딜러 커뮤니티 — `community`, 전기차 정비 네트워크 — `industry`)를 채워 문서가 말하는 "type은 엄격한 단일 enum이 아닌 참고용 분류"를 그대로 반영했다. `Store.getGroups()`/`getGroup(id)`/`createGroup({name,type,description}, createdByUserId)`를 추가했다. 카마스터의 `region`(문자열) 필드를 `groupIds`(배열)로 교체했다 — 기존 `'부산/울산'`처럼 한 문자열에 두 지역을 욱여넣던 것을, `['g_busan','g_ulsan']` 두 개의 실제 그룹 참조로 분리했다.

**UI**: 카마스터 온보딩(신규 가입) 화면과 관리자의 대리 온보딩 폼에서 "활동 지역" 텍스트 입력을 그룹 카탈로그 기반 **다중 선택 체크박스**로 바꿨다(승인 절차 없이 즉시 반영 — 1.3절 "그룹 가입 자체는 승인 없이 바로 처리"). 고객의 카마스터 검색 화면은 groupIds를 그룹명으로 변환해 그대로 보여준다(이준호 카마스터가 이제 "부산, 울산" 두 개로 표시됨 — 브라우저로 확인). 관리자 "통합 사용자" 탭 상단에 그룹 카탈로그 섹션(목록 + 생성 폼)을 추가했다 — 그룹 생성은 슈퍼바이저만 가능하다는 원칙에 따라, 슈퍼바이저/커뮤니티관리자가 아직 분리되지 않은 이 데모에서는 관리자 계정이 슈퍼바이저를 겸하는 것으로 간주했다.

**영향받은 파일**: `store.js`(`groups[]`+`DEFAULT_GROUPS`+3개 메서드 신설, `DEFAULT_KARMASTERS` 및 `onboardKarmasterAndFillContract` region→groupIds 전환), `karmaster.js`/`admin.js`(그룹 체크박스 헬퍼 신설, 온보딩 폼 2곳 교체), `customer.js`(카마스터 검색 카드 표시 갱신). 테스트 1개(`test_unregistered_karmaster_onboarding.js`)의 `#ob-region` 텍스트 입력을 `#ob-grp-g_gyeonggi` 체크박스 클릭으로 갱신. 기존 테스트 9개 모두 통과.

**의도적으로 미룬 부분**: `groupIds`의 실시간 자동 유추(v1.7 — 계약 이력에서 브랜드/지역을 자동 추출해 채우는 것)는 구현하지 않았고, 지금은 온보딩 시점에 사람이 직접 고르는 방식으로만 채워진다. Shop에도 `groupIds`를 붙이는 것(4.3절)은 Shop 독립 레지스트리 작업(다음 순번) 때 함께 다룬다. 커뮤니티관리자의 "배정된 Group으로 조회 범위 제한" 같은 권한 스코핑은 슈퍼바이저/커뮤니티관리자 분리 작업(별도 순번)으로 미룬다.

## v6.15 (2026-08-25, 통합 User + 역할 속성(roleAttributes) 도입 — 겸임 원칙 최소 구현)

`user-account-role-model-spec.md`의 핵심 원칙 "한 사람 = 하나의 계정(Identity), 그 계정에 0개 이상의 역할 속성(Role Attribute)이 붙는다"(1.1절)를 처음 반영했다. 지금까지 카마스터/시공업체/배송기사/구매자는 서로 완전히 분리된 배열(또는 구매자의 경우 계정 자체가 없음)이라 같은 사람이 여러 역할을 겸임해도 서로 무관한 별개로 취급됐다(문서 2장 AS-IS 분석 그대로). 이 슬라이스는 그 중 가장 근본적인 축인 "전화번호 기반 통합 식별자"만 우선 들여왔다 — Group/Shop 레지스트리, 실명 비노출, OTP 인증 등 나머지 계정 모델 요소는 각각 별도 슬라이스로 미룬다.

**데이터 모델**: `store.js`에 최상위 `users[]`를 신설했다. `Store.touchUserRole(phone, name, role)`이 전화번호를 정규화해 기존 User를 찾거나 새로 만들고, 아직 없는 역할이면 `roleAttributes[]`에 `{role, active, attachedAt}`을 추가한다(이미 있으면 중복 추가하지 않음). `getUserByPhone`/`getUsers()` 접근자를 함께 추가했다.

**부착 시점**: 문서 1.6절("구매자 역할 속성은 로그인이 아니라 최초 계약 등록 시점에 자동 부착")을 그대로 따라 `startContractRequest`/`createContractRecordDirect`(계약 등록 성공 시)에서 `customer` 역할을, `onboardKarmasterAndFillContract`(초대·자발적 가입 두 경로 모두 이 함수로 합류)에서 `karmaster` 역할을 부착한다. 카마스터/시공업체/배송기사는 각 화면의 `tryLogin`이 성공할 때(로그인 시점) 해당 역할을 부착한다.

**겸임(상호주의) 시연**: 카마스터가 다른 카마스터를 통해 개인적으로 신차를 구매하면, 로그인 때 생긴 `karmaster` 역할과 계약 등록 때 생긴 `customer` 역할이 전화번호가 같으므로 같은 `User` 아래 함께 쌓인다 — 문서 1.2절의 "카마스터 A가 개인 차량을 구매" 예시를 브라우저에서 그대로 재현해 확인했다. 관리자 화면에 신설한 "통합 사용자" 탭(`renderUsersTab`)에서 전화번호별 보유 역할과 "겸임중" 배지를 볼 수 있다.

**진입 구조(1.5/1.6/4.4절)**: `index.html`(이 저장소 자체 QA 런처)의 역할 카드에서 관리자·배송기사를 뺐다 — 문서가 "관리자는 셀프 온보딩 대상이 아니라 시작 화면에 카드를 아예 올리지 않는다", "배송기사는 메뉴만 숨기고 기능은 유지한다"고 명시한 원칙을 반영한 것이다. 다만 이 파일은 실제 서비스 시작 화면이 아니라 5개 화면을 한 번에 열어보는 내부 QA 런처라, 완전히 없애지 않고 "내부 테스트용 바로가기" 섹션으로 분리해 남겨뒀다(관리자/배송기사 두 html 모두 URL로는 그대로 접근 가능 — 기능 제거가 아니라 진입 동선만 뺀 것).

**영향받은 파일**: `store.js`(`users[]` 신설, `touchUserRole`/`getUserByPhone`/`getUsers` 추가, 계약 등록·카마스터 온보딩 함수 3곳에 부착 호출 삽입), `karmaster.js`/`shop.js`/`driver.js`(`tryLogin`에 부착 호출 추가), `admin.js`(통합 사용자 탭 신설), `index.html`(역할 카드 재배치). 순수 추가 기능이라 기존 동작을 바꾸지 않았고, 기존 테스트 9개 모두 수정 없이 통과. 브라우저로 카마스터 로그인 → 같은 번호로 계약 등록 → 관리자 "통합 사용자" 탭에서 겸임 표시 확인까지 전체 시나리오를 직접 실행해 검증했다.

**의도적으로 미룬 부분(가장 큰 잔여 범위)**: `Group`(커뮤니티/지역) 엔티티와 그 카탈로그, `Shop`을 독립 레지스트리로 재정의하고 사업자등록 진위확인 API 연동하는 것, `UnclaimedKarmasterProfile`(미가입 카마스터 평판 분리 테이블), 닉네임 전역 유일성 + 표시명(닉네임/실명) 토글 + 브랜드 정책에 따른 실명 강제 비노출, 슈퍼바이저/커뮤니티관리자 권한 분리, OTP 등 실제 인증. 이들은 각각 그 자체로 상당한 규모라 별도 슬라이스로 남겨둔다.

## v6.14 (2026-08-25, DELIVERED 화면 검수/전자서명 폼 신설)

`new-car-delivery-tracking-ui-items-spec.md` 1.2절("도착/인수 완료 화면")의 CUSTOMER 입력 항목을 반영했다 — 지금까지는 오너가 버튼 한 번("차량 수령 확인하기")만 누르면 곧바로 최종인수승인이 끝났는데, 실제로는 검수 결과·전자서명이 그 승인의 전제조건이어야 한다는 문서 요구사항을 채운 것이다.

**데이터 모델**: 예약에 `deliveredAt`(DELIVERED 전환 시각, `_arriveReservation`에서 기록), `inspectionResult`(`'ok'`\|`'issue'`), `inspectionNote`, `customerPhotos[]`, `signature`(dataURL) 필드를 추가했다. 신규 `Store.submitCustomerInspection(id, {...})`가 검수결과/메모/사진/서명을 한 번에 저장한 뒤 기존 `confirmDelivery`(고객 최종인수승인) 로직을 그대로 이어 타 DELIVERED→CONFIRMED 양방향 게이트를 그대로 재사용한다 — 관리자의 `forceConfirmDeliveryBoth`(대리처리)는 검수 데이터 없이 이 게이트를 직접 건드리므로 영향받지 않는다.

**고객 화면**: `renderDeliveryConfirm`을 신설해 인수 사진 갤러리 아래에 검수 라디오(이상없음/이슈발견, 이슈 선택 시 메모 500자 필수 노출) + 선택적 사진 첨부(샘플/파일 업로드) + 캔버스 기반 전자서명 패드 + [인수 확인] CTA를 추가했다. 서명·검수 조건이 모두 채워지기 전까지 CTA는 비활성 상태다. 서명은 store 데이터가 아니라 화면에만 있는 상태라 — 카마스터가 같은 건을 개인수령확인 처리하는 등 다른 창에서 발생한 store 변경으로 전체 재렌더가 걸려도 그려둔 서명이 사라지지 않도록, 예약번호별 dataURL 캐시(`signatureCache`)에 매 스트로크마다 저장해뒀다가 캔버스가 다시 그려질 때 그 위에 복원하는 방식을 썼다(브라우저로 강제 재렌더를 일으켜 직접 검증함). 제출 완료 후에는 검수 결과·메모·서명 이미지를 요약해서 보여준다.

**영향받은 파일**: `store.js`(필드 4종 + `submitCustomerInspection`), `customer.js`(`renderDeliveryConfirm`/`attachSignaturePad` 신설, 기존 `doConfirmDelivery` 제거 — 더는 쓰이지 않아 죽은 코드가 됨). 테스트 3개(`test_no_service_flow.js`/`test_karmaster_shop_monitor.js`/`test_full_service_flow.js`)의 "차량 수령 확인하기" 단일 클릭을 "이상없음 선택 → 캔버스에 실제 마우스 드래그로 서명 → 인수 확인" 시퀀스로 갱신(Playwright의 실제 마우스 이벤트로 캔버스에 그려 서명 로직 자체를 검증). 기존 테스트 9개 모두 통과.

**의도적으로 미룬 부분**: 고객 촬영 사진(`customerPhotos`)에 회수(내리기) 기능은 두지 않았다(스펙에도 명시되지 않음, 매니저 사진과 달리 고객 스스로 제출 전에 로컬에서만 다루다가 한 번에 올리므로 회수 UI의 필요성이 낮다고 판단). 500자 제한은 `maxlength` 속성으로만 걸었고 서버측(Store) 트리밍 검증은 하지 않는다.

## v6.13 (2026-08-25, hasCustomizing/destinationType 필드명 정식 전환)

`new-car-delivery-tracking-service-spec.md` 4.1절의 `DestinationType`(`DEALERSHIP`/`AFFILIATED_SHOP`/`CUSTOM_ADDRESS`) 어휘를 정식으로 도입하고, 기존 `needsService` 불리언 필드를 제거했다.

**데이터 모델**: 예약(`reservation`)의 `needsService` 필드를 없애고 `destinationType`(문자열 Enum, 탁송 계약 시점에 카마스터가 확정)으로 대체했다. `hasCustomizing`(구 `needsService`)은 더 이상 별도로 저장하지 않고 `destinationType === 'AFFILIATED_SHOP'`에서 파생시키는 함수(`hasCustomizing(r)`)로 전환했다 — 두 값이 서로 어긋날 수 있는 상태 자체를 데이터 모델에서 없앤 것이다. `DESTINATION_TYPE_OPTIONS`(3종)와 라벨 헬퍼(`destinationTypeLabel`)를 store.js에 신설했다.

**UI**: 카마스터 승인 화면(`renderApprovalReview`)·미등록 카마스터 온보딩(`renderOnboard`)·관리자의 3개 대리처리 폼(계약 대리 등록/미등록 카마스터 대리 승인/계약 대리 승인)에서 기존 "시공 필요/불필요" 2지선다 버튼을 "영업소 직행/제휴 시공소 경유(커스터마이징)/고객 지정 장소로 배송" 3지선다로 교체했다. 지정업체명 입력란은 "제휴 시공소 경유" 선택 시에만 보이도록 했다(이전에는 "시공 필요" 시에만 보였던 것과 동일한 원칙, 조건만 갱신). 고객·카마스터·관리자 화면의 "출고 후 시공예정: 필요/불필요" 표시는 모두 "목적지 유형: <라벨>"로 바꿨다.

**의도적으로 유지한 단순화**: 문서의 조합표는 `AFFILIATED_SHOP` 완료 후 목적지가 "영업소"로 고정된다고 설명하지만, 이 MVP는 v6 원 설계 원칙("고객이 최종 수령지를 자유롭게 정한다")을 그대로 유지해 목적지 문자열(`deliveryAddress`)은 여전히 출고 요청 시점에 고객이 자유롭게 입력한다 — `destinationType`은 분류 값일 뿐, 카마스터의 실제 "영업소 주소"를 저장하는 데이터 모델(현재 `karmasters[]`에 없음)은 추가하지 않았다. 이 단순화는 "필드명 정식 전환"이라는 요청 범위에 맞춘 의도적 선택이며, 실제 영업소 주소 개념이 필요해지면 별도 작업으로 다뤄야 한다.

**영향받은 파일**: `store.js`(필드/헬퍼 교체), `karmaster.js`/`admin.js`(3지선다 폼 4곳, 공용 `destinationTypeButtonsHTML`/`wireDestinationTypeButtons` 헬퍼 각 파일에 신설), `customer.js`(표시 텍스트 갱신). 테스트 7개의 버튼 셀렉터를 갱신(`km-need-y-`/`km-need-n-`/`a-nc-need-y`/`ob-need-y` → `km-dest-AFFILIATED_SHOP-`/`km-dest-DEALERSHIP-`/`a-nc-dest-AFFILIATED_SHOP`/`ob-dest-AFFILIATED_SHOP` 등). 기존 테스트 9개 모두 통과. 브라우저로 `CUSTOM_ADDRESS`(신규 3번째 옵션, 기존엔 없던 경로) 흐름을 직접 실행해 `hasCustomizing` 파생과 지정업체명 필드 숨김을 확인했다.

## v6.12 (2026-08-25, Layer 2 — 매니저 보강 정보 입력/게시 신설)

`new-car-delivery-tracking-service-spec.md` 3.2절/7장의 3-Layer 모델 중 Layer 2(매니저 보강)를 반영했다 — v6.11에서 배송 Enum만 교체했을 뿐 실제 부가 정보를 입력하는 화면은 없었던 것을 채우는 작업이다. Layer 1(현대글로비스 RPA 스크래핑)은 이 MVP에 대응하는 실제 외부 소스가 없어 범위 밖으로 남긴다.

**데이터 모델**: 예약(`reservation`)에 `augmentation` 객체를 신설했다 — `draft`(초안, 미게시)/`published`(게시분)로 텍스트성 필드(ETA·위치코멘트·지연사유코드+메모·커스터마이징 공정현황)를 분리하고, 기사 성명·연락처·내부 특이사항 메모는 애초에 게시 대상이 아니므로 `draft`에만 존재한다(`Store.saveAugmentationDraft`/`publishAugmentation`). 인수 완료 사진·커스터마이징 현장 사진은 문서의 예외 원칙대로 업로드 즉시 게시되고 회수(내리기)만 가능하다(`addDeliveryPhoto`/`withdrawDeliveryPhoto`/`addCustomizingPhoto`/`withdrawCustomizingPhoto`, `withdrawn` 플래그로 표시). 지연 사유 코드 9종(`DELAY_REASON_OPTIONS`)은 ui-items-spec.md 2.2절의 자체 제정안을 그대로 채택했다.

**카마스터 화면**: 배송 진행 중(`IN_TRANSIT`/`CUSTOMIZING`/`DELIVERED`/`EXCEPTION`)인 예약 상세에 "배송 보강 정보 입력" 패널을 신설했다(`renderAugmentationPanel`) — 타이핑 중에는 로컬 변수에만 담아두고 [초안 저장]/[게시하기]를 눌러야 Store에 반영되는, 기존 검색창 등과 동일한 패턴이다. 사진 업로드 패널(`renderPhotoPanel`)은 샘플 이미지 생성과 실제 파일 업로드(shop.js와 동일한 FileReader 패턴)를 모두 지원하며, 회수된 사진은 카운트만 별도로 보여주고 목록에서 사라진다.

**고객 화면**: `renderTransit`(최종 목적지행 IN_TRANSIT)에 게시된 ETA/위치코멘트 배너를 추가했고, `shop_progress`(CUSTOMIZING) 화면을 고정 문구 대신 게시된 공정 현황 텍스트 + 실시간 사진 갤러리로 교체했다(1.5절 "커스터마이징 진행 화면" 반영 — 기존 v6 정책이던 "카마스터-업체 간 별개 거래라 실시간 상세 비노출"은 업체명·정확한 위치에는 여전히 적용되지만, 공정 현황·사진은 문서 v1.5가 명시적으로 노출 대상으로 바꿨다). `exception` 화면은 게시된 지연 사유 코드+메모로 교체했다(게시 전에는 "사유가 확인되는 대로 안내됩니다" 문구로 대체). `delivery_confirm`(DELIVERED) 화면에는 인수 사진 갤러리를 추가했다. 지정업체행 1차 구간(`shop_transit`)은 정책대로 그대로 최소 정보만 노출한다(게시된 정보가 있어도 보여주지 않음).

**관리자 화면**: 카마스터용 상세 입력 폼을 그대로 옮기지 않고, "배송 보강 정보 대리 게시(샘플)" 버튼 하나로 ETA·위치코멘트(및 해당 시 공정현황/지연사유)를 즉시 게시하는 대리처리 액션을 추가했다(`renderLayer2QuickActions`) — 신속 진행이라는 관리자 화면의 존재 이유에 맞춘 축약형이다.

**버그 수정(같은 작업 중 발견)**: `renderStatusStepperHTML`(store.js)의 EXCEPTION 배지가 카마스터 전용 내부 트리거 사유(`r.exceptionReason`)를 고객 화면에도 그대로 노출하고 있었다 — 이 스텝 바는 고객·카마스터 화면이 공유하는 컴포넌트인데, 게시 게이트를 거치지 않은 내부 메모가 새고 있었던 것. 스텝 바에서는 구체적인 사유 문구를 아예 빼고 "지연 중"이라는 중립적 사실만 표시하도록 고쳤다 — 각 화면(카마스터=내부 사유, 고객=게시된 사유)이 자기 문맥에 맞는 텍스트를 각자 보여준다.

**의도적으로 미룬 부분**: Layer 1(RPA 자동 수집, 실제 외부 소스 부재), 전자서명·검수 폼(1.2절 도착 화면의 CUSTOMER 입력 항목), "원본 대조 참고 영역"(Layer 1 부재로 대조할 원본이 없음), 인수 사진 업로드를 개인수령확인의 필수 조건으로 강제하는 것(문서는 "조건부 필수"이나 데모 흐름 마찰을 피하려 소프트 힌트로만 유지).

**영향받은 파일**: `store.js`(`augmentation` 데이터 모델, 지연사유 옵션, Layer 2 Store 메서드 7개, `renderStatusStepperHTML` 정보 유출 수정), `karmaster.js`(보강 입력 패널·사진 업로드 패널 신설), `customer.js`(IN_TRANSIT/CUSTOMIZING/DELIVERED/EXCEPTION 화면에 게시분 반영), `admin.js`(대리 게시 액션 신설). 기존 테스트 9개 모두 통과(동작 변경 없이 신규 UI만 추가돼 기존 테스트는 갱신 불필요).

## v6.11 (2026-08-25, 신차인도서비스 배송 단계를 신차배송조회 설계 문서 기준 Enum으로 전환)

`vlp/car-lifecycle-cowork/docs/new-car-delivery-tracking-service-spec.md`(v1.5)가 정의한 현대글로비스 실제 5단계 기반 상태 Enum(`READY`/`DISPATCHED`/`IN_TRANSIT`/`CUSTOMIZING`/`DELIVERED`/`CONFIRMED`/`EXCEPTION`)을 신차인도서비스(`reservation.stage`)의 배송 단계에 반영했다 — mvp_v6 재설계의 첫 착수 항목이다. 기존 mvp_v5 코드는 고정 4체크포인트 타이머 시뮬레이션과 서비스 레벨 stage(계약등록~인도완료)가 뒤섞여 있었다(문서 8장 AS-IS 분석 참고).

**상태 전환**: `계약확정` 이후의 흐름을 `requestRelease` 한 번의 호출 안에서 READY(출고처리)·DISPATCHED(탁송사인수) 이력을 즉시 남기고 바로 `IN_TRANSIT`(탁송중)부터 실제 배송 시뮬레이션 타이머를 시작하도록 설계했다 — 실제 글로비스 5단계 순서는 지키되, 기존 v5 타이밍(출고 의뢰 즉시 배송 시작)과 완전히 호환되도록 READY/DISPATCHED에 별도 대기시간을 두지 않았다(Playwright 테스트의 짧은 `waitForSelector` 타임아웃과 충돌하지 않기 위한 의도적 선택). 기존 `시공중`은 `CUSTOMIZING`으로, `수령확인대기`/`인도완료`는 `DELIVERED`/`CONFIRMED`로 명칭을 바꿨다.

**DELIVERED → CONFIRMED 양방향 확인 게이트(신규 동작)**: 기존에는 오너 단독 확인(`confirmDelivery`)만으로 즉시 `인도완료`로 전환됐지만, 문서 4.1/4.2절 기준으로 "카마스터 개인수령확인"과 "고객 최종인수승인"이 **양쪽 다** 완료돼야 `CONFIRMED`로 전환되도록 바꿨다(`Store.confirmManagerReceipt`/`confirmDelivery`/`_maybeConfirm`, 순서 무관). 관리자 대리처리는 `forceConfirmDeliveryBoth`로 한 번에 양쪽을 처리한다. 이 동작 변경으로 인해 `test_no_service_flow.js`/`test_karmaster_shop_monitor.js`/`test_full_service_flow.js`에 카마스터의 "개인수령확인 처리" 클릭 단계를 추가했고, `test_admin_fasttrack.js`의 버튼 텍스트를 갱신했다.

**EXCEPTION(지연/예외) 신규**: 카마스터가 `IN_TRANSIT`/`CUSTOMIZING` 중 수동으로 "지연/예외 표시"를 눌러 사유를 남길 수 있다(`Store.markException`/`clearException`) — 예외 상태인 동안은 `_tick`의 배송 자동 도착 처리가 멈추고(시계가 "일시정지"됨), 해제 시 그동안 흐른 시간만큼 transit 시작시각을 보정해 재개한다.

**신차배송조회 상태 스텝 바(신규 UI)**: `store.js`에 `renderStatusStepperHTML`/`deliveryPhaseIndex`를 신설해, 고객·카마스터 화면에 READY~CONFIRMED(커스터마이징 경로는 최대 7단계)를 보여주는 진행 스텝 바를 추가했다 — `hasCustomizing`(기존 `needsService`) 여부에 따라 `IN_TRANSIT`이 두 번(1차 지정업체행/2차 최종목적지행) 등장하는 것을 `transitStage`(`TO_SHOP`/`TO_DESTINATION`)로 구분해 인덱스를 계산한다. 기존 `.dstepper` CSS를 그대로 재사용했다.

**영향받은 파일**: `store.js`(상태 Enum·양방향 확인 게이트·EXCEPTION·스텝 바 헬퍼 신설, `_tick`/`_arriveReservation`/`requestRelease`/`confirmKarmasterShopDone` 갱신), `customer.js`(`computePhase`/DELIVERED·EXCEPTION 렌더러 갱신, 상단에 상태 스텝 바 삽입), `karmaster.js`(`stageStepLabel`/저니박스 전면 갱신, 개인수령확인·지연예외 폼 신설), `admin.js`(대리처리 분기 갱신, 상태 배지에 `stageDisplayLabel` 적용). `driver.js`/`shop.js`는 `stage` 필드를 직접 참조하지 않아 변경 없음. 테스트 4개 갱신, 기존 테스트 9개 모두 통과.

**아직 반영하지 않은 부분(다음 착수 후보)**: Layer 2(`ManagerAugmentation`, 기사 연락처/ETA/위치 코멘트/지연 사유 등 매니저 보강 입력 + 게시 게이트), 인수 완료 사진/전자서명/검수 폼, `hasCustomizing`/`destinationType` 필드명 자체의 전면 도입(현재는 기존 `needsService`/`karmasterShopName`을 그대로 유지) — `new-car-delivery-tracking-service-spec.md`/`-ui-items-spec.md` 참고.

## v6.10 (2026-08-06, 카마스터/시공업체/배송기사 로그인 화면을 연락처 입력 방식으로 재설계)

카마스터·시공업체·배송기사 로그인 화면이 계정 카드를 여러 개 늘어놓고 그중 하나를 클릭하는 방식이었다 — 실제 서비스에서는 있을 수 없는 흐름이라, 데모를 볼 때마다 "이게 진짜 로그인처럼 보이지 않는다"는 인상을 줬다. 고객 로그인과 동일하게 **연락처 입력 + 로그인 버튼**을 기본 화면으로 바꾸고, 테스트 편의를 위해 그 아래 평소엔 접혀 있는 `<select>` 드롭다운 하나("테스트 계정으로 빠른 로그인")만 덧붙였다 — 드롭다운에서 계정을 고르면 즉시 로그인된다.

이를 위해 시공업체(`DEFAULT_SHOPS`)와 배송기사(`DEFAULT_DRIVERS`)에도 카마스터처럼 `phone` 필드를 추가하고, `Store.getShopByPhone`/`Store.getDriverByPhone` 조회 함수를 신설했다. 관리자 로그인은 애초에 계정이 하나뿐이라 카드 나열 방식에 문제가 없어 그대로 뒀다.

**영향받은 파일**: `store.js`(샵·기사 데이터에 `phone` 추가, 조회 함수 2개 신설), `karmaster.js`/`shop.js`/`driver.js`(로그인 화면 전면 재설계, 공용 `loginPhoneFormat` 헬퍼). 테스트 8개의 로그인 진입 셀렉터를 `.km-card`/`.shop-card` 클릭에서 `#quick-login` 드롭다운 선택으로 갱신(`test_booking_approval.js`, `test_full_service_flow.js`, `test_interested_car_handoff.js`, `test_karmaster_shop_monitor.js`, `test_messaging.js`, `test_no_service_flow.js`, `test_unregistered_karmaster_onboarding.js`). 기존 테스트 9개 모두 통과.

## v6.9 (2026-08-06, 카마스터 지정업체 경유(A-경로) 배송 반영 + UX 리뷰 버그 수정 일괄)

사용자 리뷰에서 나온 지적들을 한 번에 반영했다.

**카마스터 지정업체(A-경로)가 배송 여정에 실제로 반영되지 않던 문제**: "출고 후 시공예정"이 선택된 계약이라도, 지금까지 신차인도서비스 자체의 배송은 공장에서 곧장 고객이 지정한 최종 수령지로 직행했다 — 카마스터가 지정한 협력업체를 경유한다는 사실이 배송 여정 어디에도 반영되지 않았다. 이번에 2구간 배송을 도입했다: 공장→지정업체(1차, `legKind:'to_shop'`)→[카마스터가 업체와 소통해 시공완료 확인]→최종 목적지(2차, `legKind:'to_owner'`). 이 지정업체 관계는 **철저히 카마스터-업체 간 별개 거래**로 취급한다 — 신차 케어 서비스(고객-시공사)와는 무관하며, 고객은 업체명도, 실시간 배송 상세도 알 수 없다(1차 구간은 "이동중"이라는 정보만 안내). 카마스터가 승인/온보딩 시점에 선택적으로 "지정업체명"을 입력할 수 있게 했고(`store.js`의 `karmasterShopName` 필드), 신차인도서비스 예약이 `시공중`이라는 새 단계에 머무는 동안 카마스터가 "시공완료 확인 →"을 눌러야 2차 구간이 시작된다(`Store.confirmKarmasterShopDone`). needsService가 false인 건은 기존과 동일하게 1구간으로 곧장 진행된다.

**계약확정 화면 요약 정보 보강**: 계약 확인 직후(`release_prep`)에는 트림·색상·계약일자·상담메모까지 다 보여주다가, 그다음(계약확정) 화면에서는 "차량: OOO" 한 줄로 퇴화하던 회귀 버그를 고쳤다 — 이제 승인 직후와 동일한 수준의 요약을 유지한다.

**계약내역 등록 화면에 취소 버튼 부재**: 처음 화면으로 돌아갈 방법이 없던 문제를 "← 처음으로" 버튼으로 해결했다.

**신차 케어 서비스 2차 배송 문구·버그**: "출차완료 — 재배송 대기중"이라는 혼란스러운 문구를 "배송 대기중"으로 정정했다. 시공업체의 "배송 시작(오너에게)" 버튼이 클릭 후에도 비활성화되지 않아 반복 클릭 시 배송기사가 계속 재배정되고 처리 이력에 중복 로그가 쌓이던 버그를 고쳤다 — `c.transit.active`인 동안은 버튼 대신 배송 진행 카드를 보여주도록 우선순위를 바꿨다.

**시공업체가 최종 수령확인 여부를 볼 수 없던 문제**: 시공업체 대시보드에 "처리 완료" 건수 KPI는 있었지만 실제 리스트가 없어, 차를 넘긴 뒤 고객이 무사히 수령확인까지 마쳤는지 확인할 길이 없었다. 오너 수령확인 완료/대기 상태를 보여주는 리스트 섹션을 추가했다.

**영향받은 파일**: `store.js`(`karmasterShopName` 필드, 체크포인트를 `legKind` 기반으로 재구성, `confirmKarmasterShopDone` 신설, `confirmDelivery`의 stale 로그 문구 제거), `customer.js`(`shop_transit`/`shop_progress` 단계 추가, `release_prep` 요약 보강, 등록 폼 취소 버튼, `renderTransit`을 `tp.destination` 기반으로 일반화, "출차완료" 문구 수정), `karmaster.js`(지정업체명 입력 필드, "시공완료 확인" 버튼, `stageStepLabel`/저니박스 갱신, 죽은 aftermarket 안내문 제거), `admin.js`(대리처리에 `시공중` 단계 추가, 수령확인 버튼 문구 단순화), `driver.js`(1차/2차 구간 라벨·목적지를 `tp.destination` 기반으로 통일), `shop.js`(배송중 우선순위 가드, "처리 완료" 리스트 신설). 테스트 2개 갱신(`test_full_service_flow.js`, `test_admin_fasttrack.js`) — A-경로 2구간 배송 스텝을 반영. 기존 테스트 9개 모두 통과.

## v6.8 (2026-08-06, 신차 케어 서비스를 최상위 독립 엔티티로 승격)

v6.7에서 신차 케어 서비스를 needsService와 논리적으로 분리했지만, UI 구조는 여전히 그걸 감추고 있었다 — 유일한 진입점이 특정 신차인도서비스 예약의 상세화면("신차 케어 서비스 신청하기" 버튼) 하나뿐이었고, 데이터도 `r.aftermarket`이라는 부모 예약에 얹힌 필드였다. 논리는 독립이라면서 구조는 여전히 "출고 과정의 한 항목"처럼 보였던 셈이다. 이번 개편으로 신차 케어 서비스를 `data.careOrders[]`라는 완전히 별개의 최상위 목록으로 승격시켰다.

**데이터 모델**: `store.js`의 `_emptyStore()`에 `careOrders: []`, `careSeq: 0`을 추가했다. `requestCareOrder({ reservationId, shopId, mode, packageId, optionIds, customRequest })`가 신청 시점에 원본 예약(`reservationId`)에서 `customer`/`carModel`/`trim`/`color`를 그대로 복사해와(스냅샷) 저장한다 — 부모 예약과 연결이 끊긴 뒤에도(예: 신차인도서비스가 이미 인도완료로 종료된 뒤라도) 시공사가 차량 기본정보를 여전히 볼 수 있어야 하기 때문이다. `getCareOrders()`/`getCareOrdersByPhone()`/`getCareOrdersByReservation()`/`getCareOrder()` 등 예약과 대칭되는 접근자를 신설했고, 계약번호도 자체 카운터(`careSeq`, `20-YYYYMM-NNNN`)를 쓴다.

**"내 차량" 기반 신청 흐름**: 고객 화면에 신차 케어 서비스 전용 랜딩(`renderCare`)을 새로 만들어, 신차인도서비스로 등록한 모든 예약("내 차량", 출고 전이든 이미 받은 차든 상관없이)을 후보로 보여준다. 신청은 "내 차량" 중 하나를 고르는 것으로 시작하며(`renderCareSetup`), 차가 한 대뿐이면 자동 선택된다. 여러 대일 때는 목록에서 직접 고른다(1대 제약을 두지 않았다 — 고객 한 명이 여러 신차인도서비스 계약을 가질 수 있으므로). 신차인도서비스 계약확정 화면의 "신차 케어 서비스 신청하기" 버튼은 이제 그 자리에서 바로 신청 폼을 여는 게 아니라, 해당 차량을 미리 선택한 채로 이 독립된 신청 흐름("신차 케어 서비스로 이동")에 진입시키는 역할만 한다.

**입고 확인을 시공사의 명시적 액션으로**: 예약과 완전히 분리되면서 "그 차가 신차인도서비스 배송에 얹혀 도착했다"는 신호를 자동으로 감지할 방법이 없어졌다(애초에 둘이 같은 배송일 필요도 없다 — 고객이 나중에 직접 몰고 갈 수도 있다). 대신 시공사가 계약 완료(`confirmed`) 건에 대해 스스로 "입고 확인 →" 버튼을 눌러야(`Store.confirmCareDropoff`) 시공 흐름이 시작되도록 바꿨다 — 차가 실제로 어떻게 왔는지는 시공사 입장에서 상관할 필요가 없다는 원칙.

**배송·타임라인·평가 로직의 다형성 확보**: `_buildTransit`/`_tick`/`transitProgress`/`renderDeliveryStepperHTML`/`renderHistoryLogHTML`/`renderShopTimelineHTML`을 "예약 또는 케어주문, 둘 중 무엇이든" 다룰 수 있게 일반화했다. `submitRating(targetType, ...)`도 `targetType==='karmaster'`면 예약을, 아니면 케어주문을 대상으로 분기한다. `driver.js`는 이제 예약의 1차 배송과 케어주문의 2차 배송(시공 완료 후 오너 재배송)을 한 목록에 함께 보여준다.

**잔존했던 needsService-목적지 결합 로직 정리**: 이번 승격 작업 중 `store.js`의 `requestRelease`가 여전히 `r.needsService`로 신차인도서비스 자체 배송의 목적지("시공업체"/"오너")를 나누고 있던 걸 발견했다 — v6.7에서 "출고 후 시공예정"과 "신차 케어 서비스"를 논리적으로 분리했다고 했지만 이 부분은 놓쳤던 흔적이다. 실제로는 그 배송의 목적지는 고객이 입력한 `deliveryAddress` 하나로만 정해져야 한다. 체크포인트 문구도 목적지 종류가 아니라 출발지(공장 vs 시공업체) 기준으로 재정의했다(`CHECKPOINTS_FROM_FACTORY`/`CHECKPOINTS_FROM_SHOP`). `karmaster.js`에 남아있던 죽은 `r.aftermarket` 참조(단계 라벨, 배송 목적지 표시, 진행현황 안내 박스)도 함께 제거했다 — 카마스터는 애초에 신차 케어 서비스에 전혀 관여하지 않는다는 원칙에 맞춘 정리다.

**관리자 화면 재구성**: `admin.js`를 신차인도서비스/신차 케어 서비스 두 개의 독립 탭으로 나눴다(`setAdminTab`). 각 탭이 자기 목록·상세·대리처리 UI를 따로 갖는다 — 신차인도서비스 상세에는 연결된 케어주문이 있으면 참고용으로 보여주되(관리자는 전체를 총괄하므로), 실제 처리는 케어 서비스 탭에서 독립적으로 이뤄진다.

**영향받은 파일**: `store.js`(전면 재작성 — `careOrders` 모델, 관련 함수 일체, 체크포인트 재정의), `customer.js`(전면 재작성 — `care`/`care_setup`/`care_detail` 뷰 신설, `computePhase` 단순화), `shop.js`(전면 재작성 — `Store.getCareOrders()` 직접 조회, "입고 확인" 버튼 추가), `admin.js`(전면 재작성 — 탭 구조), `driver.js`(예약+케어주문 배송 통합 목록), `karmaster.js`(잔존 aftermarket 결합 로직 제거). 테스트 3개 갱신(`test_full_service_flow.js`, `test_admin_fasttrack.js`, `test_booking_approval.js`). 기존 테스트 9개 모두 통과.

## v6.7 (2026-08-06, 출고 요청을 고객→카마스터 2단계로 재구성 + 최종 수령지 지정 + 신차 케어 서비스 완전 분리)

계약이 확정됐다고 곧바로 배송이 시작되는 게 아니다 — 지금까지는 카마스터가 알아서 "출고 요청" 버튼을 누르는 단일 액션이었는데, 실제로는 **고객이 먼저 "이제 차를 받고 싶다"는 의사표시로 최종 수령지와 함께 출고를 요청하고, 그걸 받은 카마스터가 비로소 공장에 출고를 의뢰하는 2단계**여야 한다. 계약 등록이 "오프라인 계약 → 온라인 계약"에 준하는 소통의 장을 여는 것일 뿐, 그 자체로 출고를 트리거하지는 않는다는 원칙을 반영했다.

**최종 수령지는 출고 전까지 고객이 자유롭게 정한다**: 카마스터의 "출고 후 시공예정 여부"(구 시공필요/불필요)는 카마스터가 지정한 협력업체에서 진행하는 옵션 작업 여부이며 오프라인 계약 시점에 이미 정해지는 것이지만, 실제 차량을 최종적으로 받을 곳은 이것과 무관하게 고객이 정한다 — 자택일 수도, 개인적으로 알아서 맡기려는 정비·시공업체일 수도 있다. `store.js`에 `deliveryAddress` 필드를 추가해 `requestOwnerRelease(id, deliveryAddress)`가 이 값을 받아 저장하고, `customer.js`의 출고 요청 화면에 "최종 수령지" 입력창을 추가했다.

**신차 케어 서비스를 needsService와 완전히 분리**: 지금까지는 "출고 후 시공예정"이 true인 건에서만 신차 케어 서비스 신청 화면(`am_setup`)에 진입할 수 있었는데, 이건 다시 두 개념을 섞는 것이었다 — 카마스터 옵션시공 여부와 무관하게 고객은 언제든 독립적으로 시공사를 골라 신차 케어 서비스를 시작할 수 있어야 한다. `customer.js`의 `computePhase`가 이제 `r.aftermarket` 유무만으로 애프터마켓 단계를 판단하며(needsService 조건 제거), 출고 준비 화면(`release_prep`, 구 `contract_confirmed_noservice`)에 "신차 케어 서비스 신청하기" 진입점을 추가해 그 안에서 바로 시작할 수 있게 했다. 이에 따라 `canRequestRelease(r)`도 신차 케어 서비스 계약 완료 여부를 더 이상 확인하지 않는다 — 출고 가능 여부는 오직 고객의 출고 요청 제출 여부로만 정해지고, 신차 케어 서비스를 이번 배송의 목적지로 쓰고 싶다면 그건 고객이 최종 수령지에 그 업체 주소를 직접 입력하는 것으로 표현한다.

`karmaster.js`에는 고객이 아직 요청하지 않았을 때 보여줄 안내와, 요청 후 최종 수령지를 보여주는 출고 의뢰 버튼("공장에 출고 의뢰하기 →")을 추가했다. `admin.js`의 대리처리 경로에도 "고객 출고 요청 대리 처리" 단계를 끼워 넣고, 신차 케어 서비스 대리 등록 조건에서도 needsService 제약을 없앴다.

**참고**: 카마스터가 지정하는 협력업체(출고 후 시공예정, A 경로)는 여전히 실제 업체 정보(이름·주소·진행상황)를 기록할 데이터 모델이 없다 — 지금은 목적지 타입("시공업체"/"오너")만 구분할 뿐 구체적인 업체 식별은 신차 케어 서비스(아프터마켓 shopId)를 통해서만 가능하다. 이 부분은 별도로 더 다듬을 여지가 있다.

**영향받은 파일**: `store.js`(`deliveryAddress` 필드, `requestOwnerRelease` 시그니처 변경, `canRequestRelease` 단순화), `customer.js`(`computePhase`의 needsService 분리, `release_prep`/`am_confirmed_wait` 재작성, `renderReleaseBox` 공용 함수 신설, `renderTransit`/`delivery_confirm`의 aftermarket null 안전성 보강), `karmaster.js`, `admin.js`, 테스트 6개(`test_full_service_flow.js`, `test_no_service_flow.js`, `test_admin_fasttrack.js`, `test_karmaster_shop_monitor.js`) — 최종 수령지 입력, 신차 케어 서비스 진입 버튼, 바뀐 문구에 맞춰 셀렉터 갱신. 기존 테스트 9개 모두 통과.

## v6.6 (2026-08-06, "애프터마켓" → "신차 케어 서비스"로 명칭 통일 + 시공옵션 라벨 정정)

**개념 정리**: 신차인도서비스(카마스터-고객 계약) 안의 "출고 후 시공예정 여부"는 색상·트림처럼 계약에 딸린 단순 옵션(예상 정보)일 뿐이고, 실제 시공은 그 옵션과 무관하게 고객이 시공사와 별도로 맺는 완전히 독립된 계약이다. 그런데 이 옵션의 UI 라벨이 카마스터의 승인/온보딩 화면과 고객의 계약확정 요약에서 "시공(애프터마켓) 필요 여부"라고 표시되고 있었다 — "애프터마켓"이라는 단어가 들어가는 바람에 마치 카마스터가 정하는 이 예/아니오가 곧 애프터마켓 계약 자체인 것처럼 읽혔다. 라벨을 **"출고 후 시공예정 여부"**로 바꿔 이 옵션이 신차인도서비스 쪽 정보일 뿐임을 분명히 했다(`karmaster.js`의 승인/온보딩 폼과 요약 라인, `customer.js`의 계약확정 요약).

**명칭 통일**: "애프터마켓"이 일부 화면에서는 "애프터마켓(신차 케어 서비스)"로 병기되고 다른 화면에서는 "애프터마켓"만 쓰이는 등 일관되지 않았다. 라벨 혼동의 근본 원인이기도 해서, 고객·카마스터·시공사·관리자 화면 전체에서 "애프터마켓"이라는 표현을 없애고 **"신차 케어 서비스"로 통일**했다 — 화면 제목, 상태 배지, 안내 문구, 처리 이력(log) 메시지, KPI 라벨까지 전부 반영했다(`customer.js`/`karmaster.js`/`shop.js`/`admin.js`/`store.js`). 내부 코드 식별자(`aftermarket` 필드명, `requestAftermarketQuote` 등 함수명, `POINT_REWARD_AFTERMARKET` 상수명)는 사용자에게 노출되지 않으므로 그대로 뒀다.

덤으로 관리자 KPI의 "코드확인 대기" 라벨도 정정했다 — v6.3에서 코드 검증 방식을 필드 검토·승인 방식으로 바꾸면서 실제로는 "카마스터 승인 대기" 건수를 세고 있었는데 라벨만 예전 그대로 남아 있었다.

**영향받은 파일**: `customer.js`, `karmaster.js`, `shop.js`, `admin.js`, `store.js`(로그 메시지 포함), `README.md`, 테스트 3개(`test_admin_fasttrack.js`, `test_full_service_flow.js`) — 바뀐 화면 텍스트를 찾는 셀렉터를 새 문구로 갱신. 기존 테스트 9개 모두 통과.

## v6.5 (2026-08-06, 로그인 정보 자동입력 + 계약번호 체계 개편 + 애프터마켓 독립 번호)

**로그인한 계정 정보를 계약내역 등록 폼에 자동 반영**
고객이 이미 전화번호로 로그인해 신원이 확인된 상태인데도 계약내역 등록 화면에서 "내 이름/내 연락처"를 다시 타이핑해야 했다. 데모 로그인(`renderLoginMock`)에 이름 입력을 추가하고, 그 값을 등록 폼의 기본값으로 채우도록 했다(`customer.js`) — 다만 실제 계약 당사자가 로그인한 사람과 다를 수 있어(예: 가족 명의 계약) 값은 계속 수정 가능하게 뒀고, 자동 입력된 경우에는 그 사실과 "다르면 수정하라"는 안내를 필드 아래에 덧붙였다. 로그인 화면에서 이름·연락처를 비워두고 진행해도(기존 데모/테스트 흐름과 동일) 기존처럼 직접 입력하는 경로가 그대로 살아있다.

**예약번호 체계를 `SS-YYYYMM-NNNN` 형식으로 개편**
기존의 단순 순번(R0001, R0002…) 대신 `10-202608-0001`처럼 세 구간으로 나눴다(`store.js`의 `_fmtReservationId`). `SS`는 서비스유형(10=신차인도서비스, 20=애프터마켓), `YYYYMM`은 앱에 등록(디지털화)된 연월, `NNNN`은 그 서비스유형 내 전체 누적 순번이다. 처음엔 두 번째 구간에 계약 상대방(카마스터/시공사)의 고유번호를 넣는 방안도 검토했지만, 그러면 고객이 보는 번호와 카마스터가 보는 번호가 서로 다른 값을 "상대방 코드"로 넣어야 해서 같은 계약을 두고 서로 다른 두 번호가 존재하는 문제가 생긴다는 지적에 따라 폐기했다 — 등록 연월은 양쪽 모두에게 항상 같은 값이라 이 문제가 없다.

**애프터마켓 계약에 독립된 번호 부여**
"신차인도서비스와 완전히 독립된 계약"이라는 원칙에 맞춰, 애프터마켓도 부모 예약번호에 얹혀 있던 것을 그만두고 자체 계약번호(`20-...`, 별도 누적 카운터)를 갖도록 했다(`requestAftermarketQuote`에서 발급). 고객 화면 상단(`내 예약번호` 옆), 시공업체 대시보드의 각 작업 카드 제목에 이 번호를 노출했다.

**계약 검색 목록의 날짜 컬럼을 등록일시 → 계약일자로 변경**
고객의 "내 계약 확인" 목록에서 앱 등록 시각(`createdAt`)보다 실제 계약서상의 계약일자(`contractDate`)가 계약을 식별하는 기준으로 더 실질적이라고 판단해 컬럼을 교체했다(`customer.js`의 `renderHistory`).

**영향받은 파일**: `store.js`(`_fmtReservationId` 신설, `startContractRequest`/`createContractRecordDirect`/`requestAftermarketQuote`가 새 포맷 사용, `aftermarket.id` 필드 추가), `customer.js`(로그인 폼에 이름 추가, `requestDraft` 기본값에 로그인 정보 반영, 헤더에 애프터마켓 번호 표시, 계약 목록 날짜 컬럼 교체), `shop.js`(작업 카드 제목을 부모 예약번호 대신 애프터마켓 자체 번호로 표시). 기존 테스트 9개 모두 그대로 통과(하드코딩된 예약번호 포맷에 의존하는 테스트가 없었음).

## v6.4 (2026-08-06, 고객/카마스터 화면에 계약 검색·필터 추가)

한 사람(고객 또는 카마스터)이 동시에 여러 건의 계약을 갖고 있을 때, 그 중 특정 건을 빠르게 찾을 수 있도록 두 화면 모두에 좁혀보기 기능을 추가했다. 인증 방식 자체는 건드리지 않았다 — 카마스터는 로그인, 고객은 전화번호 조회로 이미 자기 소관 목록을 확보한 뒤, 그 목록 안에서만 필터링한다(다른 사람의 계약을 검색해서 찾아낼 수 있는 통로는 추가하지 않았다).

**카마스터 대시보드**: 담당 고객 목록 위에 계약번호·고객이름·연락처로 실시간 검색되는 입력창을 추가했다(`karmaster.js`). 이 입력은 매 글자마다 전체 화면을 다시 그리는 `render()`를 호출하기 때문에, 그 과정에서 입력창 자신이 포커스를 잃지 않도록 `_renderInner()`에 포커스·커서 위치 복원 로직을 추가했다 — 이 로직이 없었다는 건 기존 `#ob-token`(미등록 카마스터 온보딩) 입력에도 있던 잠재 버그였는데, 이번에 같이 고쳤다.

**고객 "내 계약 확인" 화면**: 전화번호로 조회된 결과가 2건 이상일 때만 계약번호·카마스터 이름·차종으로 좁혀보는 입력창을 노출한다(`customer.js`). 카마스터 이름을 한눈에 구분할 수 있도록 결과 테이블에 카마스터 열도 추가했다. 이 입력은 전체 화면 `render()`를 타지 않고 결과 영역만 다시 그리는 로컬 함수(`renderTable`)를 쓰기 때문에 별도의 포커스 복원 로직이 필요 없다.

**영향받은 파일**: `karmaster.js`(`kmSearchQuery` 상태 추가, `_renderInner()`에 포커스 복원 로직 추가, 담당 고객 목록에 검색창 추가), `customer.js`(`renderHistory()`에 결과 2건 이상 시 노출되는 필터 입력과 카마스터 열 추가). 기존 테스트 9개 모두 그대로 통과 확인(검색창은 검색어가 비어 있는 기본 상태에 필터를 걸지 않는 추가 UI라 기존 셀렉터 기반 흐름에 영향 없음).

## v6.3 (2026-08-06, 임의 코드 검증 → 계약 필드 검토·승인 방식으로 전환)

v6.2까지는 카마스터가 요청을 열려면 고객에게 받은 6자리 확인 코드를 타이핑해야 했다. 이 흐름을 다시 짚어보니, 전제가 명확해졌다 — 이 단계는 새로운 관계를 시작하는 게 아니라 **이미 오프라인에서 체결된 계약**을 디지털로 연결하는 절차다. 그렇다면 검증 수단도 "임의로 발급한 비밀번호"보다 "실제 계약서에 적힌 내용 자체"가 더 적절하다.

**차량 식별 필드를 계약 시작 시점에 고객이 입력**
계약내역 등록 화면에 차량 모델·트림·색상·계약일자 필드를 추가했다. 이 조합(차량 스펙 + 정확한 계약일자 + 당사자 이름·연락처)은 실제로 그 계약서를 양쪽 다 들고 있어야만 맞출 수 있는 지문 역할을 한다 — 딜러 연락처 하나만으로는 증명이 안 됐지만(검색 화면에 이미 공개돼 있어서), 이 조합은 임의의 제3자가 우연히 다 맞힐 수 없다.

**등록된 카마스터: 코드 입력 게이트 제거, "검토 후 승인"으로 대체**
카마스터가 별도 코드를 타이핑할 필요 없이, 고객이 등록한 계약 내용이 대시보드에 바로 뜬다. 카마스터는 화면에 나온 차량정보·계약일자·고객정보가 실제로 자신이 체결한 계약과 맞는지 눈으로 확인한 뒤 승인하면 된다(`renderApprovalReview`, 구 `renderCodeGate` 대체) — 시공 필요 여부·상담 메모만 이 시점에 추가로 입력한다. 사람이 직접 검토·승인하는 게 숫자 대조보다 실질적인 검증이라고 판단했다.

**미등록 카마스터: "조회번호"로 재정의**
발급되는 6자리 값은 그대로 유지하되, 역할을 "보안 비밀값"에서 "그 요청을 찾기 위한 조회번호"로 명확히 했다(`getReservationByToken`). 미등록 카마스터도 이 번호로 요청을 찾으면 고객이 입력한 계약 내용을 그대로 검토할 수 있고, 신뢰는 번호를 아는 것 자체가 아니라 이어지는 화면에서의 검토·승인에서 나온다.

**초대는 항상 실제 계약 당사자가 개인적으로** — 재확인
등록 카마스터에게 조회번호를 입력하라고 요구하지 않게 된 것과 별개로, 미등록 카마스터에게 앱이 자동으로 초대(SMS/카카오톡 등)를 보내는 것은 채택하지 않았다. 계약이 이미 체결되어 있더라도, 특정 개인에게 접촉을 시작하는 주체는 항상 실제 계약 당사자(고객, 또는 반대로 카마스터가 미가입 고객에게 구두로 권유하는 경우도 마찬가지)여야 한다는 원칙을 유지했다 — 채널(문자냐 인앱이냐)이 아니라 "누가 시작하는가"가 핵심이기 때문이다.

**영향받은 파일**: `store.js`(`startContractRequest`에 `carModel`/`trim`/`color`/`contractDate` 파라미터 추가, `fillContractDetails`에서 `carModel` 파라미터 제거, `onboardKarmasterAndFillContract`에서 `carModel` 파라미터 제거), `customer.js`(계약내역 등록 폼에 차량정보 필드 추가, 등록된 카마스터 대상일 땐 조회번호 화면 대신 "승인 대기" 안내로 대체), `karmaster.js`(코드 입력 폼 제거, 계약 내용 검토+승인 폼으로 교체, 미등록 온보딩 폼에서 차량정보 재입력 제거하고 읽기전용 검토로 대체), `admin.js`(대리처리 폼도 동일하게 검토+승인 방식으로 갱신). 테스트 7개 갱신(`npm test` 9개 전부 통과).

## v6.2 (2026-08-06, 카마스터 찾기와 계약 요청 분리 + 미등록 카마스터 온보딩)

v6.1의 "카마스터 찾기" 카드에 "계약 요청 시작" 버튼을 바로 붙여둔 게, 날짜/시간 입력만 빠졌을 뿐 예전 온라인 예약 흐름(탐색 → 클릭 → 바로 연결)과 사실상 같다는 지적을 반영했다. 동시에, 상담한 카마스터가 아직 이 서비스에 등록되지 않은 경우도 처리할 수 있도록 요청 시작 방식 자체를 바꿨다.

**"카마스터 찾기"는 순수 리서치용으로, 연결 버튼 제거**
검색 화면(`renderKarmasterSearch`)에서 "이 카마스터와 계약 요청 시작" 버튼을 없앴다. 이제 이 화면은 평점 조회만 가능하고, 실제 계약 요청은 완전히 별도 화면("계약 요청 시작하기")에서 시작한다.

**계약 요청은 "목록에서 고르기"가 아니라 "이미 아는 연락처 입력하기"로**
고객이 계약 요청을 시작할 때 카마스터를 목록에서 클릭하는 대신, 오프라인 상담에서 이미 알게 된 **카마스터의 연락처를 직접 타이핑**한다(`Store.startContractRequest`). 입력한 번호가 등록된 카마스터와 일치하면 그 카마스터에게 바로 연결되고, 일치하지 않으면(미등록 카마스터) `karmasterId` 없이 연락처(`pendingKarmasterPhone`)와 토큰만 저장해둔다. 이렇게 하면 "탐색하다 바로 연결"이라는 예전 패턴이 구조적으로 불가능해지고, 미등록 카마스터 케이스도 같은 입력 하나로 자연스럽게 갈라진다.

**토큰의 의미 재정의: 1회성 검증값 → 이 계약 건의 지속 창구**
기존의 "확인 코드"를, 카마스터-고객이 이 계약 건에 대해 계속 쓰는 창구로 재해석했다. 요청 생성 시 자동으로 "계약 정보를 입력해 주세요" 메시지가 대화 스레드에 먼저 꽂힌다. 토큰은 앱이 대신 전달하지 않는다 — 화면에는 복사 버튼만 두고, 실제 전달(문자·전화 등)은 고객이 직접 한다. 이것도 "카마스터 개인 대상 온라인 접촉을 앱이 대신하지 않는다"는 원칙을 계약 시작 단계까지 일관되게 적용한 것이다.

**미등록 카마스터: 토큰만으로 가입과 동시에 자동 연결**
카마스터 로그인 화면에 "아직 계정이 없으신가요? 토큰으로 첫 계약 등록하기"를 추가했다(`renderOnboard`). 토큰을 입력하면 고객이 이미 입력해둔 이름·연락처가 그대로 확인되고, 카마스터는 이름·지역·계약 내용만 입력하면 `Store.onboardKarmasterAndFillContract()`가 신규 카마스터 레코드 생성과 계약 내용 기록을 한 번에 처리하며 자동 로그인까지 이어진다. 별도의 이중 확인(코드+연락처 재입력) 없이 토큰 하나로 충분하다고 판단했다 — 토큰 자체가 고객이 그 사람에게만 전달한 비밀값이기 때문이다. 관리자(총괄)도 실제 고객요청 건 중 미등록 카마스터 케이스를 동일하게 대리 처리할 수 있다.

**의도적으로 만들지 않은 것: "암호화된 키"**
카마스터 식별자를 실제로 암호화/서명하는 것은 이번 범위에 넣지 않았다. 지금 프로토타입은 브라우저 localStorage만으로 동작해 보호할 실질적인 암호화 경계가 없고, 겉모습만 그럴듯한 문자열을 만들면 오히려 실제 백엔드 설계 시 기대치를 헷갈리게 할 수 있다고 판단했다. 대신 지금의 평문 연락처를 그대로 식별 키로 쓰고, 실제 백엔드에서는 이 값이 서명된 토큰으로 바뀌어야 한다는 점만 README에 명시했다.

**영향받은 파일**: `store.js`(`getKarmasterByPhone`/`startContractRequest`/`getReservationByToken`/`onboardKarmasterAndFillContract` 추가, `createContractRequest` 제거), `customer.js`(검색 화면에서 버튼 제거, "계약 요청 시작" 화면을 연락처 직접 입력 방식으로 전면 수정, 토큰 복사 버튼 추가), `karmaster.js`(로그인 화면에 미등록 카마스터 온보딩 진입점 추가), `admin.js`(미등록 카마스터 케이스 대리처리 추가). 기존 테스트 7개 갱신 + 신규 `test_unregistered_karmaster_onboarding.js` 추가 (총 9개, `npm test` 통과).

## v6.1 (2026-08-06, 계약 시작 주체를 고객으로 전환 — 확인 코드 기반 핸드오프)

v6에서 카마스터가 고객 이름·연락처를 직접 입력해 계약을 만들던 구조를 재검토했다. 계약의 실제 당사자는 고객인데 시작 주체가 카마스터가 되어 있었고, 오탈자나 사칭으로 엉뚱한 사람과 연결될 보안 위험도 있었다. 이제 계약은 반드시 고객이 시작한다.

**계약 요청(고객 시작) → 확인 코드 → 카마스터 기록으로 재구성**
고객이 "카마스터 찾기" 화면에서 상담한 카마스터를 지정해 계약 요청을 만들면(본인 이름·연락처 직접 입력, `Store.createContractRequest()`), 6자리 확인 코드가 발급된다(`_genCode()`). 고객이 이 코드를 오프라인에서 카마스터에게 전달하면, 카마스터는 담당 고객 목록에서 해당 건을 열어 코드를 검증해야만(`Store.fillContractDetails()`) 계약 내용(차종·시공여부·상담메모)을 기록할 수 있다 — 코드가 일치하지 않으면 제출 버튼이 비활성 상태로 남는다. 고객 이름/연락처는 이미 고객이 입력해둔 값을 그대로 보여줄 뿐, 카마스터가 다시 입력하지 않는다. `test_booking_approval.js`에서 서로 다른 고객의 코드가 교차 사용되지 않는지도 함께 검증한다.

**카마스터의 자유 입력 폼 제거, 관리자 신속처리는 예외로 유지**
카마스터 화면의 "+ 새 계약 등록" 자유 입력 폼을 완전히 없애고, 목록에서 건을 선택하면 코드 확인 화면(`renderCodeGate`)이 뜨는 구조로 바꿨다. 다만 관리자(총괄)는 데모 신속처리라는 별도 권한이 있으므로, 고객요청→코드확인 2단계를 생략하고 한 번에 등록하는 기존 방식(`Store.createContractRecordDirect()`)을 예외로 유지했다 — 다만 관리자가 실제 고객이 시작한 요청 건을 대신 처리할 때는 카마스터와 동일하게 코드 확인 절차를 대리한다(관리자는 코드를 직접 조회할 수 있다는 점만 다르다).

**메시지 채널은 계약 확정 전에도 열려 있음**
확인 코드 검증 전(고객요청 단계)에도 고객·카마스터 메시지 패널은 그대로 노출된다 — "온라인 개별 영업행위 금지"가 "고객 응대 금지"를 뜻하지 않는다는 원칙을 계약 시작 이전 단계에도 일관되게 적용했다.

**영향받은 파일**: `store.js`(`createContractRequest`/`fillContractDetails`/`createContractRecordDirect` 추가, `createContractRecord` 제거), `customer.js`(카마스터 카드에 "계약 요청 시작" 버튼 + 요청 폼 + 코드 발급 화면 신규), `karmaster.js`("+ 새 계약 등록" 제거, 코드 확인 게이트로 대체), `admin.js`(실제 고객요청 건에 대한 코드확인 대리처리 추가). Playwright 테스트 7개 갱신, 1개(`test_admin_fasttrack.js`)는 관리자 전용 경로가 그대로라 변경 없이 통과.

**버그 수정: `.btn-primary.btn-sm` 조합 버튼의 글자가 안 보이던 문제**
새로 만든 "이 카마스터와 계약 요청 시작" 버튼을 브라우저에서 직접 확인하다 발견했다. `app.css`에서 `.btn-sm`이 `.btn-primary`보다 소스 순서상 나중에 정의되어 있어(둘 다 특이도가 같음) `.btn-sm`의 흰 배경이 `.btn-primary`의 파란 배경을 덮어써, 흰 배경에 흰 글자(`.btn-primary`가 지정한 `color:#fff`만 남음)로 버튼 글자가 안 보이는 상태였다. `master`에도 이미 있던 문제였고(`admin.js`/`karmaster.js`/`shop.js`의 대리처리·제출 버튼 다수가 영향받음), `button.btn-primary.btn-sm`/`button.btn-danger.btn-sm` 조합에 대한 명시적 규칙을 추가해 고쳤다.

## v6 (2026-08-06, 서비스 구조 재편 — 신차인도서비스 / 애프터마켓 분리)

제조사(현대기아) 정책상 카마스터/딜러 개인이 온라인 채널로 별도 영업행위(상담 예약 잡기 등)를 진행할 수 없다는 제약을 반영해, 서비스 구조 자체를 다시 짰다. 기존에 하나의 파이프라인으로 묶여 있던 "차량 인도"와 "시공(애프터마켓)"이 책임 소재가 다른 두 개의 독립된 서비스로 분리되었다. `master` 브랜치는 v5.2 상태로 보존하고, 이번 개편은 `v6-service-split` 브랜치에서 진행했다.

**상담 예약의 온라인 절차 제거 → "계약 등록(카마스터 1차 기록 → 고객 확인)"으로 대체**
기존의 "고객이 앱에서 카마스터를 골라 날짜·시간을 예약 → 카마스터가 앱에서 승인" 흐름(예약요청/예약확정/상담완료 3단계)을 통째로 제거했다. 카마스터 검색은 앱에서 평점 기반으로 하되(`renderKarmasterSearch`), 실제 상담 일정 조율은 오프라인(전화·문자)으로 이뤄진다. 오프라인 상담이 끝나면 카마스터가 `Store.createContractRecord()`로 계약 내용(차종·시공여부·상담메모)을 앱에 1차 기록하고, 고객이 `confirmContractByCustomer()`로 확인하면서 신차인도서비스가 시작된다.

**신차인도서비스 / 애프터마켓(신차 케어 서비스) 완전 분리**
기존에는 "시공"이 계약 확정 화면 안에서 패키지·시공업체를 함께 고르는 하나의 옵션이었지만, 이제는 완전히 독립된 계약이다. 시공사는 카마스터와 달리 온라인에서 바로 검색·선택·견적요청까지 가능하다(`requestAftermarketQuote` → `respondAftermarketQuote` → `confirmAftermarketQuote`). 애프터마켓 계약이 완료돼야만 카마스터의 "출고 요청"이 가능하도록 `canRequestRelease()`에 게이트를 걸었다 — 배송 목적지(시공업체 주소)가 그 계약으로 확정되기 때문이다.

**카마스터 역할 경계 재정의: "차량인도 완료 = 목적지 도착 확인" 시점에 종료**
차량이 목적지(시공업체 또는 오너)에 도착해 오너가 확인하는 순간(`confirmDelivery`) 카마스터의 신차인도서비스 역할이 끝난다. 시공이 있는 건이라도 이후 진행(입고~작업~검수~2차배송~수령)은 카마스터가 모니터링하지 않는다 — 카마스터 화면에서 시공 진행 타임라인 섹션 자체를 제거했다. 다만 이건 "온라인 개별 영업행위 금지"이지 "고객 응대 금지"가 아니므로, 오너-카마스터 메시지 채널은 역할 종료 후에도 계속 열려 있다.

**추가옵션 카탈로그 + 시공 전 견적 확정**
"추가 요구사항은 비용 처리 없이 현장에서 별도 협의"하던 방식을 걷어내고, 표준 옵션 카탈로그(`OPTION_CATALOG`, 항목명+가격)에서 다중 선택하도록 바꿨다. 시공사가 패키지+옵션+요청사항을 보고 최종 견적가를 회신하면, 고객이 확인해야 계약이 완료된다 — 작업 착수 전에 가격이 확정되는 구조. 사후의 "정찰제 확인"은 이제 이 사전 확정 견적과 실제 청구액을 대조하는 본래 취지에 맞게 남았다.

**다차원 평점 + 서비스별 독립 포인트 적립**
카마스터·시공사 각각 3개 항목(카마스터: 전문성/응대속도/친절도, 시공사: 시공품질/일정준수/가격정확도)으로 평가받는다(`RATING_DIMS_KARMASTER`/`RATING_DIMS_SHOP`, `Store.submitRating`). 신차인도서비스(카마스터 평가)와 애프터마켓(시공사 평가)은 완전히 독립적으로 포인트가 적립되며, 평가는 완료 즉시 남길 수도 있고 "내 계약 확인" 이력에서 나중에 남길 수도 있다(강제하지 않음). 제공자별 고정 보너스 포인트(`bonusPoint`) 필드를 마련해뒀지만 값은 0으로 시작 — 건별 입력이 아니라 업체가 서비스 가입 시 계약으로 정하는 고정값이라는 설계만 반영하고, 실제 운영 화면은 다음 단계로 남겼다.

**영향받은 파일**
`store.js`(데이터 모델·상태머신 전면 재작성), `customer.js`(카마스터 검색·계약확인·애프터마켓 신청/견적확인·평가 화면 신규), `karmaster.js`(계약 등록 폼 신규, 출고요청 게이트, 시공 모니터링 섹션 제거), `shop.js`(견적 응답 단계 신규), `admin.js`(계약 대리 등록 포함 전 과정 대리처리 갱신), `driver.js`(필드 참조만 조정). Playwright 테스트 8개 전부 새 흐름에 맞게 재작성(`npm test` 통과).

## v5 후속 변경 2 (2026-08-04, 화면 흐름 조율 — 상세 피드백 반영)

실제 화면 흐름을 검토하며 나온 세부 피드백을 코드에 반영했다.

**출고 요청을 카마스터의 명시적 액션으로 되돌림**
계약 확정과 동시에 출고까지 자동 처리하던 것을 되돌렸다. "언제 출고를 요청했는지"가 이력에 남아야 하고, 카마스터가 최종 계약 내용(시공업체 포함)을 확인한 뒤 출고를 개시하는 게 실제 흐름과 맞다는 지적을 반영했다. `confirmContract()`는 이제 `'계약확정'` 상태에서 멈추고, 이전부터 있었지만 쓰이지 않던 `Store.requestRelease()`를 카마스터 화면의 "출고 요청하기" 버튼(관리자 화면엔 "출고 요청 대리 처리")에 연결해 배송을 시작하도록 했다.

**차량확인대기 게이트를 오너 단독 확인으로 단순화**
시공업체 도착 후 오너·카마스터가 각각 확인해야 작업이 시작되던 것을, 오너 단독 확인만으로 즉시 작업이 개시되도록 바꿨다. 카마스터는 "차량이 도착해서 대기 중"이라는 상태만 보고 별도로 확인 버튼을 누를 필요가 없다 — 도착과 확인이 사실상 한 동작이라는 지적을 반영했다.

**고객검수대기·수령대기 게이트에 확인 순서를 강제**
오너가 만족하지 않고 재작업을 요청했는데도 카마스터가 그와 무관하게 확인을 눌러 다음 단계로 넘길 수 있던 문제를 막기 위해, 오너가 먼저 확인(또는 이의제기)해야 카마스터의 확인이 유효하도록 `confirmReceipt()`에 순서 가드를 추가했다. 오너 확인 전에는 카마스터 화면에 확인 버튼 자체가 뜨지 않는다.

**시공업체 단계명 "최종검수" → "작업완료" (화면 표시만)**
뒤이은 고객의 "고객검수" 단계와 용어가 겹쳐 혼동을 줄 수 있다는 지적을 반영했다. 내부 상태값(`code: '최종검수'`)은 다른 로직이 참조하고 있어 그대로 두고, 화면에 노출되는 버튼 라벨(`short` 필드)만 "작업완료"로 바꿨다.

**시공업체 청구액 입력을 정찰가/추가금액/사유로 분리, 일괄 기록으로 변경**
"정찰가와 동일" / "정찰가+5만원" 버튼을 누를 때마다 즉시 이력이 쌓이던 방식을, 기본 금액(정찰가, 읽기전용) + 추가 금액(입력) + 추가 사유(선택 텍스트)를 다 채운 뒤 "고객 검수요청 보내기"를 누르는 시점에 한 번만 기록하도록 바꿨다. 실제로는 작업을 마친 뒤 한 번에 청구하는 게 맞다는 지적을 반영했다. 사진 첨부는 이전 "작업중" 단계의 업로드 기능을 그대로 활용하도록 남겨뒀다(같은 단계에 중복 업로더를 두지 않았다).

**시공업체 화면의 입고 전 배송 스텝퍼 제거**
시공업체 입장에서는 배송 경로의 구간별 위치까지 볼 필요 없이 "도착했는지"만 중요하다는 지적을 반영해, "입고 예정" 목록의 상세 스텝 인디케이터를 단순 상태 문구로 대체했다.

**5개 화면에 로그인 화면 예시 추가**
카마스터·시공업체에만 있던 "카드 클릭 로그인" 데모 화면을 구매자·배송기사·관리자 화면에도 추가했다. 실제 인증은 하지 않고 클릭 한 번으로 통과하는 목업이지만, 데모/발표 시 로그인 화면의 존재 자체를 보여줄 수 있다.

**그 외 소소한 정리**
"배송·처리 이력"을 "전체 처리 이력"으로 명칭 통일(상담 예약 단계 로그도 포함하므로), 상담 메모 입력란 라벨을 "추가 상담 내용"에서 "상담 메모"로(읽기 전용 표시 라벨과 통일), 패키지 카드에 포함 항목을 나열하고 기능 없는 "이 매장 선택" 버튼 제거(카드 자체가 이미 클릭 가능), 추가 요구사항 입력란에 "비용은 앱에서 처리하지 않고 시공업체와 별도 협의" 안내 추가.

## v5 후속 변경 (2026-08-04, 화면 흐름 조율)

**배송 스텝 커넥터에 진행 애니메이션 추가**
체크포인트 사이 연결선이 회색→초록으로 순간 전환만 되어, 화면을 열었을 때 이미 마지막 단계 직전까지 "순간 이동"한 것처럼 보일 수 있다는 지적을 반영했다. `transitProgress()`가 현재 체크포인트 구간 안에서의 진행률(`segPct`)도 반환하도록 하고, 연결선 안에 그 비율만큼 채워지는 fill 요소를 두어 300ms 티커가 갱신할 때마다 CSS transition으로 부드럽게 자라나도록 했다. 이제 다음 체크포인트로 넘어가는 과정 자체가 눈에 보인다.

**캐시백(계좌입력) → 포인트 적립으로 개편**
기존 "정찰가 일치 확인 시 계좌정보를 입력해 3만원 캐시백 신청" 구조를 재검토했다. 원 기획서의 캐시백은 금융상품 연계 혜택인데, 실제 구현은 이전 GV80 프로토타입의 UI를 그대로 재사용하면서 의미가 붕 떠 있었고, 데모 단계에서 실제 계좌번호를 평문으로 입력·저장하는 것도 불필요한 위험이었다. 정찰가 일치가 확인되는 즉시(계좌정보 입력 없이) 포인트를 적립하고, 전화번호를 키로 하는 포인트 지갑(`Store.getPointBalance`/`_adjustPoints`)에 쌓이도록 바꿨다. 적립된 포인트는 다음 계약 확정 시 결제 금액에서 현금처럼 차감해 쓸 수 있다(`renderContractServiceForm`의 "포인트 사용" 입력란, `confirmContract`가 사용액만큼 지갑에서 차감). 계좌정보 입력 절차 자체가 없어지면서, 이전에 "캐시백 신청은 관리자가 대신할 수 없다"로 시연하던 대리처리 경계도 함께 사라졌다(관리자도 정찰제 확인과 동시에 포인트를 대신 적립할 수 있음).

## v5 (2026-08-04)

### v4에서 v5로 — 근본적인 재구성

v4까지는 "GV80 시공 프로토타입"의 뼈대를 그대로 끌고 와서, 시공(애프터마켓) 단계 위주로 짜여 있었습니다.
실제로 필요한 건 **차량 구매 상담부터 출고, 실시간 배송, (필요시) 시공, 수령까지 전체 여정을 실시간으로 추적하는 것**이었고, 이번 v5는 이 구조를 처음부터 다시 짰습니다.

### 추가 수정사항 (입력 버그 및 배송 업데이트 주체)

**1. 한글 입력/날짜 선택기 끊김 버그 수정**
0.5초마다 화면 전체를 다시 그리도록 되어 있었는데, 이 때문에 한글 조합 중(자음+모음이 완성되기 전) 입력창 DOM 자체가 통째로 교체되어 조합이 끊기고, 열려있던 날짜 선택기도 닫히는 문제가 있었습니다. 원인은 "배송 진행률을 부드럽게 보여주기 위해 매 틱마다 무조건 재렌더링"하도록 짜여 있었기 때문입니다. **실제로 데이터가 바뀐 경우에만 재렌더링**하도록 고치고, 배송 진행률 표시는 전체 재렌더링 없이 진행률 바/잔여시간 텍스트만 직접 갱신하는 별도의 가벼운 타이머로 분리했습니다. `tests/test_input_stability.js`로 이 수정이 실제로 효과가 있는지 검증했습니다 (버그가 있던 코드로 되돌려서 테스트가 실패하는 것도 확인함).

**2. 배송 상태 업데이트 주체: 카마스터 추가**
말씀하신 대로 배송기사보다 카마스터가 고객과 밀접하게 소통하는 입장이라는 점을 반영해, 카마스터 화면에도 담당 차량의 실시간 배송 현황과 "즉시 도착 처리" 버튼을 추가했습니다. 기본은 자동 시간 기반 시뮬레이션으로 진행되고, 카마스터·관리자·배송기사 누구든 필요시 즉시 도착 처리를 할 수 있습니다.

**3. 날짜/시간 입력을 `date` + `time` 분리로 변경**
`datetime-local` 입력 하나로 처리했더니 브라우저마다 달력 팝업 지원이 일관되지 않아 반응하지 않는 문제가 있었습니다. 이전 버전(v3~v4)에서 안정적으로 동작했던 `date` 입력 + 시간 드롭다운(30분 단위, 10:00~18:30) 조합으로 되돌렸습니다.

**4. 예약 화면에 "관심 차종"과 "상담 희망 내용" 필드 추가**
구매자 예약 화면에 날짜·시간만 있고 상담 내용이나 차량 관련 입력이 없어 비대칭이었던 부분을 반영했습니다. 구매자가 예약 시점에 관심 차종(선택)과 상담 희망 내용(선택)을 남기면, 카마스터 화면에 그대로 표시되고 "확정 차종" 입력란에 기본값으로 제안됩니다. 최종 확정은 여전히 상담 중 카마스터·구매자 합의로 이루어지며, 이 값은 참고용 출발점일 뿐입니다.

**5. 출고 요청 주체를 고객 → 카마스터로 변경**
계약 확정 이후 실제 수령까지(출고 → 배송 → (시공) → 재배송 → 도착)는 고객이 "관리"하는 구간이 아니라 **모니터링만 하는 구간**이어야 한다는 점을 반영했습니다. 고객 화면에서 "출고 요청" 버튼을 제거하고, 카마스터 화면에 "출고 요청 필요" 목록을 추가해 카마스터가 요청을 트리거하도록 바꿨습니다. 이전에 추가한 카마스터의 "즉시 도착 처리"와 합쳐지면서, 카마스터가 출고 요청부터 두 차례의 배송 도착 처리까지 전체 여정을 관리하는 구조가 완성됩니다. 경로 A(고객 직접선택)든 경로 B(카마스터 추천)든 동일하게 적용됩니다.

**6. 출고 요청은 사람이 누르는 액션이 아니라 자동 처리로 변경**
지난 버전에서는 출고 요청을 카마스터가 누르도록 옮겼는데, 다시 생각해보니 실제 업무 흐름과 맞지 않았습니다. 실제로 영업사원(카마스터)과 계약서를 쓰고 나면, 출고 신청은 영업점 백오피스 시스템에서 계약 확정과 동시에 자동으로 처리되는 것이 일반적입니다. 그래서 고객이든 카마스터든 별도로 "출고 요청" 버튼을 누르는 절차 자체를 없애고, **계약 확정과 동시에 출고 절차(배송 시작)까지 하나의 처리로 자동 진행**되도록 바꿨습니다. 카마스터는 그 이후 배송 현황을 모니터링하고 필요시 도착 처리에 개입하는 역할만 유지합니다.

**7. 고객 ↔ 카마스터 메시지 기능 추가**
계약~수령까지 모니터링 구간에서 고객이 할 수 있는 일이 전혀 없다는 점을 보완하기 위해, 예약 상세 화면 하단에 카마스터와 메시지를 주고받는 패널을 추가했습니다. 카마스터 화면에도 담당 건별로 메시지 스레드와 답장 입력창이 있습니다. (시공업체와의 메시지, 배송/시공 중 추가 요청, 긴급 이슈 제기 등은 다음 단계로 남겨두었습니다.)

**8. 예약 승인 절차 + 확정 메시지 + 시간대 중복 예약 방지**
"신청은 했는데 확정된 건지 모호하다"는 점을 반영해, 예약은 이제 `예약요청`(카마스터 승인 대기) → `예약확정`(명확한 확정 메시지 표시) 두 단계로 나뉩니다. 카마스터가 승인 버튼을 눌러야 고객 화면에 "예약이 확정되었습니다 ✓" 메시지가 뜹니다. 또한 같은 카마스터의 같은 날짜에 이미 잡힌 시간대는 다른 고객의 예약 화면 시간 선택지에서 자동으로 제외되어, 중복 예약 자체가 원천적으로 불가능합니다.

**9. 배송 과정 상세화: 배송기사 배정 + 위치 체크포인트 + 1차/2차 구분**
기존에는 진행률 %만 보여주고 "지금 시공업체로 가는 중인지 고객에게 가는 중인지" 구분이 흐릿했습니다. 이제 배송이 시작되면 담당 배송기사가 배정되고(2명 중 무작위), 구간별로 4단계 위치 체크포인트(예: 공장 출발 → 고속도로 이동 중 → 시공업체 인근 도착 → 시공업체 도착)가 진행률에 따라 자동으로 표시됩니다. 화면마다 "1차 배송(시공업체행)" / "2차 배송(고객 인도)"을 명확히 구분해 라벨링했습니다.

**10. 시공업체 도착 시점의 오너·카마스터 상호확인 체크포인트 추가**
1차 배송이 시공업체에 도착하면 곧바로 시공이 시작되지 않고, 기존 수령 확인과 동일한 방식으로 **오너와 카마스터가 각각 도착을 확인**해야 시공이 시작되도록 새 체크포인트(`도착확인대기`)를 추가했습니다. 최종 수령 확인과 이 중간 체크포인트는 동일한 확인 메커니즘을 재사용합니다.

**11. 슬라이드 진행바 → 단계별 스텝 인디케이터로 변경**
배송 진행을 하나의 매끄러운 슬라이드 바로 보여줬는데, "한번에 쭉 가는 것보다 단계별로 뚝뚝 끊어서 이동하는 걸 보여줬으면 좋겠다"는 의견을 반영했습니다. `.transit-bar`(% 너비 애니메이션) 대신, 체크포인트 개수만큼 원형 노드가 늘어선 스텝 인디케이터(`.dstepper`)로 바꿨습니다. 완료된 단계는 초록, 현재 단계는 파랑으로 표시되고 그 사이는 선으로 연결됩니다. 구매자·카마스터·배송기사·시공업체·관리자 화면 모두 동일한 컴포넌트를 재사용합니다.

**12. 시간대별 도착/출발 전체 이력 기록 및 표시**
배송 체크포인트를 통과할 때마다(예: "고속도로 이동 중" 진입, "시공업체 인근 도착" 등) 정확한 시각과 함께 이력에 자동 기록됩니다. 구매자 상세 화면에 "배송·처리 이력" 섹션을 추가해 전체 타임스탬프 로그를 확인할 수 있습니다.

**13. 카마스터 화면의 배송/도착확인/수령확인을 하나의 "여정" 섹션으로 통합**
기존에는 "배송 현황", "시공업체 도착 확인 필요", "수령 확인 필요"가 서로 별개의 섹션으로 나뉘어 있어서, 2차 배송(시공완료→고객) 진행 중에도 관련 없는 "시공업체 도착 확인 필요" 섹션이 계속 표시되는 문제가 있었습니다. 세 상태를 하나의 "배송 및 확인 진행" 섹션으로 합치고, 카드 제목에 항상 "1차 배송" / "2차 배송" / "1차 배송 완료" 같은 맥락을 명시해 혼동의 여지를 없앴습니다.

**14. 카마스터도 고객과 동일한 시공 진행 타임라인(사진 포함) 확인 가능**
카마스터가 전체 여정을 관리하는 주체인데 정작 시공 단계별 진행 상황(입고→작업→최종검수→출차, 현장 사진)을 볼 수 없었던 비대칭을 바로잡았습니다. 카마스터 화면에 "시공 진행 상황 (모니터링)" 섹션을 추가해 고객이 보는 것과 동일한 타임라인·사진을 볼 수 있도록 했습니다 (진행 조작은 여전히 시공업체가 담당).

**15. 카마스터 화면을 목록(좌) + 상세(우) 구조로 전면 재구성**
여러 고객의 정보와 메시지가 한 화면에 뒤섞여 있어 불편하다는 의견을 반영했습니다. 관리자 화면과 동일하게 좌측에 담당 고객 전체 목록(단계 뱃지 + 메시지 알림 포함)을 표 형태로 보여주고, 고객을 클릭하면 우측에 그 고객만의 상세 페이지가 펼쳐집니다. 상세 페이지는 ①예약 일정 ②상담 내용(메모 포함) ③계약/시공업체 정보 ④진행 현황 ⑤메시지 순서로 항목이 명확히 구분되어 있습니다.

**16. 예약 일정 확인과 상담 내용 기록을 분리 + 상담 메모 필드 추가**
"일정 승인"과 "상담 후 차종/시공여부 기록"이 헷갈린다는 지적을 반영해 두 단계를 시각적으로 분리했고, 시공유무 외에 자유롭게 남길 수 있는 "추가 상담 내용" 메모 칸을 추가했습니다.

**17. 메시지 읽음/안읽음 표시**
고객이 보낸 새 메시지가 있으면 목록에 "🔔 신규" 배지가 뜨고, 해당 고객을 선택하면 자동으로 읽음 처리됩니다.

**18. 실시간 이력을 접이식(펼침/접힘)으로 변경**
카마스터·관리자 상세 화면 모두, 전체 처리 이력을 기본적으로 접어두고 필요할 때만 펼쳐볼 수 있도록 `<details>` 토글로 바꿨습니다. 화면 부담을 줄이면서도, 고객과의 정보 공유가 필요할 때 언제든 상세 이력을 확인할 수 있습니다.

**19. 시공업체 선정 시 "추가 요구사항" 입력란 신설**
계약 단계에서 표준 패키지 외에 원하는 옵션이나 요청사항을 자유롭게 남길 수 있습니다. 비용 처리 방식은 아직 정해지지 않아, 시공업체가 이를 참고해 최종 청구액(정찰가 대조 기준값) 입력 시 반영하는 방식으로 임시 처리했습니다 — 실제 서비스에서는 옵션별 견적/승인 절차가 별도로 필요합니다.

**20. "시공업체 도착 확인"의 의미를 명확화 (차량확인대기)**
이 확인이 단순 도착 체크가 아니라 "차량 상태를 확인하고 실제 시공 착수를 승인하는 절차"라는 점이 불분명하다는 지적을 반영해, 단계명을 `차량확인대기`로 바꾸고 버튼 문구도 "차량 확인 및 작업 개시 승인"으로 명확히 했습니다.

**21. 최종검수 → 고객검수대기 → 출차완료 3단계로 재구성**
시공이 끝나자마자 바로 출차·재배송되면 품질이 불만족스러워도 되돌릴 수 없다는 지적을 반영했습니다. 시공업체가 최종검수(청구액 입력) 후 "고객 검수 요청"을 보내면, 오너·카마스터가 시공 결과(사진 포함)를 확인하고 만족 시에만 출차가 진행됩니다. 불만족 시 "재작업 요청" 버튼으로 이의를 제기할 수 있고(사유 기록), 관리자 화면에서 이의제기 현황을 확인·처리 완료 표시할 수 있습니다 (실제 서비스라면 정식 재작업 플로우가 필요합니다).

### 테스트 과정에서 발견하고 고친 버그

1. **폴링 재렌더링으로 인한 입력값 소실**: 0.5초마다 화면 전체를 다시 그리다 보니, 카마스터/관리자가 상담 완료 폼을 작성하는 도중 입력값과 선택 상태가 날아가는 문제가 있었습니다. 폼의 임시 상태를 모듈 레벨에 별도로 보관해 재렌더링에도 유지되도록 고쳤습니다.
2. **`<tr>` 요소 파싱 오류**: `<tr>`을 `<table>` 컨텍스트 없이 만들면 브라우저가 태그를 깨뜨리는 문제(v3에서 한 번 겪었던 것과 동일한 유형)가 admin.js/karmaster.js에도 있었습니다. `<table>` 컨텍스트에서 파싱하는 헬퍼로 수정했습니다.

### 인프라

- `package.json` + `tests/run-all.js` 추가: `npm test` 한 번으로 정적 서버 기동, 8개 시나리오 순차 실행, 결과 요약까지 처리. (`spawnSync`가 이벤트 루프를 막아 같은 프로세스의 서버가 응답하지 못하던 버그를 비동기 `spawn`으로 수정.)
